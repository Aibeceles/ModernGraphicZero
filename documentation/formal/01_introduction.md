# Introduction: Zeros, Differences, and Bijections

## Overview

This project explores the intersection of three mathematical domains through computational graph generation and machine learning:

1. **Newton's Forward Differences** — Iterated difference operations on polynomials
2. **Set-Theoretic Bijections** — A novel approach to establishing ℤ ↔ ℚ mappings
3. **Graph Machine Learning** — Node classification and link prediction on the resulting structure

---

## Central Claims

### Primary Claim

**Well-bounded graph generation constraints produce a graph containing the complete set of determined polynomials — corresponding to a power set structure — along with undetermined polynomials.**

Specifically:
- For every element in the power set (every possible combination of integer roots within range), there exists a graph node with corresponding root indexing (`muList`)
- Each determined polynomial's root pattern encodes as a binary number, hence maps to an integer
- The ML link prediction task can discover and order the integers represented in the graph by predicting `:NEXT_INTEGER` relationships between nodes whose binary encodings differ by 1

### The Graph as Mathematical Artifact

The graph is **generated by implementing Newton's differences**, not by implementing the bijection directly. The scientific question is whether the resulting structure lends itself to bijection-related ML tasks.

---

## Document Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           DOCUMENT FLOW                                      │
│                                                                              │
│  01_introduction ──► 02_theory ──► 03_implementation                        │
│        │                 │                │                                  │
│        │                 ▼                ▼                                  │
│        │          ┌─────────────────────────────────────┐                   │
│        └─────────►│    04_classification                │                   │
│                   │    05_machine_learning              │                   │
│                   └─────────────────────────────────────┘                   │
│                                    │                                         │
│                                    ▼                                         │
│                            06_reference                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

| Document | Purpose | Audience |
|----------|---------|----------|
| **01_introduction** | Entry point, claims, document map | All readers |
| **02_theory** | Mathematical foundations: Newton's differences, bijections | Researchers, mathematicians |
| **03_implementation** | Algorithm construction, graph schema | Developers, implementers |
| **04_classification** | Determined vs undetermined analysis | Researchers, ML practitioners |
| **05_machine_learning** | ML tasks and training approaches | ML engineers, data scientists |
| **06_reference** | Worked examples, verification, glossary | All readers (reference) |

---

## Key Concepts Summary

### Newton's Difference Algorithm

For a polynomial P(x) of degree n, the **forward difference operator** Δ:

```
Δf(k) = f(k+1) - f(k)
```

Iterated application produces the difference sequence:

```
Ψ₀ = P(x)           degree n
Ψ₁ = ΔP(x)          degree n-1
Ψ₂ = Δ²P(x)         degree n-2
...
Ψₙ = ΔⁿP(x)         degree 0 (constant = n! × leading coefficient)
```

### The Bijection Dual Mapping

Two mappings on set collections establish the ℤ ↔ ℚ correspondence:

| Mapping | Definition | Maps To |
|---------|------------|---------|
| **μ** (Set Union Ratio) | μ(Å) = \|∪A\| / Σ\|A\| | Rationals ℚ |
| **χ** (Binary Encoding) | χᵢ = 1 if position encodes set element | Integers ℤ |

### Determined vs Undetermined

A polynomial at difference level i is **determined** if:

```
totalZero == dimension - wNum
```

- **Determined:** All possible integer roots captured within evaluation range
- **Undetermined:** Missing roots (complex, irrational, or out-of-range)

---

## Production Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PRODUCTION PIPELINE                                  │
│                                                                              │
│  THEORY              IMPLEMENTATION           ML                 RELEASE    │
│  ──────              ──────────────           ──                 ───────    │
│                                                                              │
│  PDF/HTML    ──►     Java App        ──►     Zeppelin    ──►    HuggingFace │
│  (Bijection          (ZAD.jar)               Notebooks          Models      │
│   Theory)                 │                       │                          │
│                           ▼                       ▼                          │
│                      Neo4j DB            Trained Models                      │
│                    (neo4j1.dump)                                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Project Components

### Core Artifacts

| Artifact | Location | Purpose |
|----------|----------|---------|
| Mathematical Paper | `A Set-Theoretic Approach...pdf` | Formal bijection theory |
| Java Application | `ZerosAndDifferences.jar` | Graph generation from polynomial differences |
| Neo4j Database | `neo4j1.dump` | Graph storage and querying |
| ML Notebooks | `GraphicLablePrediction.json`, `GraphicLinkPrediction.json` | Training pipelines |

### Configuration Parameters

| Parameter | Example | Effect |
|-----------|---------|--------|
| `dimension` | 5 | Polynomial degree → tree depth |
| `integerRange` | 200 | Evaluation range [-100, +99] |
| `setProductRange` | 4 | Coefficient iteration → graph width |

---

## Reading Paths

### For New Readers
```
01_introduction → 02_theory (Parts I-III) → 03_implementation (Part 1) → 04_classification
```

### For Implementers
```
01_introduction → 03_implementation (full) → 06_reference (examples)
```

### For Researchers
```
01_introduction → 02_theory (full) → 04_classification → 05_machine_learning
```

### For ML Engineers
```
01_introduction → 04_classification → 05_machine_learning → 06_reference
```

---

## Quick Reference: Graph Structure

```
(:Dnode)-[:zMap]->(:Dnode)      Polynomial difference chain
(:Dnode)-[:CreatedBye]->(:CreatedBy)   Provenance tracking
```

**Key Node Properties:**

| Property | Description |
|----------|-------------|
| `vmResult` | Polynomial coefficients [aₙ, ..., a₁, a₀] in **descending** power order |
| `muList` | Root positions (adjusted indices) |
| `rootList` | True mathematical root positions |
| `n` | Numerator of μ rational (max muList value) |
| `d` | Denominator of μ rational (Σ muList). **Not polynomial degree** |
| `totalZero` | Count of detected integer roots |
| `determined` | 1 if totalZero == (dimension - wNum), 0 otherwise |

> **Note:** 
> - Polynomial degree = `dimension - wNum`, not stored as `d`
> - vmResult coefficient order determined by `MatrixA.java:transcribePowers()`. See [06_reference](06_reference.md) for examples.

---

## Next Steps

Proceed to:
- **[02_theory](02_theory.md)** for mathematical foundations
- **[03_implementation](03_implementation.md)** for algorithm details
- **[06_reference](06_reference.md)** for worked examples

---

*This document consolidates content from `abstract.md` and `organization.md` to provide a single entry point.*

