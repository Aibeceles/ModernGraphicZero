5/1

https://neo4j.com/graphacademy/training-basic-admin-40/00-basic-neo4j-admin-about/


    Describe graph components.

    Describe what a graph database is.

    Describe the Neo4j Graph Platform:

        Features of the Neo4j DBMS.

        Tools used by developers.

        Libraries, drivers, integrations used with Neo4j.

        Administrative tools.


Mathematically, a node is defined as “a point where two or more edges (relationships) meet.”
ApplesAndOranges1


The Neo4j property graph model.


Each node represents an entity
Each relationship represents how two nodes are connected

A graph database is an online database management system with Create,
Read, Update, and Delete (CRUD) operations working on a graph data model

Unlike other databases, relationships take first priority in graph databases. This means
 your application doesn’t have to infer data connections using foreign keys or out-of-band processing, 


. A Neo4j instance at a minimum contains two databases, the system database and the default database, neo4j


The system database stores metadata about the databases for the installation, as well as security configuration.

The default database (named neo4j by default) is the "user" database where you typically implement your graph
data model.

some features of Neo4j DBMS that make it different from traditional RDBMS.

index-free adjacency.

    Much fewer index lookups or table scans
    Reduced duplication of foreign keys
    An index normally includes a "key" or direct link to the original row of data

The Neo4j graph engine is used to interpret Cypher statements and also executes kernel-level code to
store and retrieve data, whether it is on disk, or cached in memory. 

The Neo4j Sandbox is way that developers can test data models with Neo4j without needing to install anything.
It is a free, temporary, and cloud-based instance of a Neo4j Server 

https://neo4j.com/labs/etl-tool/

Neo4j ETL Tool - Interactive Relational Database Data Import

It can also be run as a command line-tool

cypher-shell is part of the Neo4j installation and is located in the bin directory. 

A library is also called a plug-in as it is used to extend what can be done in Cypher.
Some libraries are available in Neo4j Desktop, while others you must download and install.


 Neo4j can be part of a system that uses GRANDstack

Administration tools

Developers and administrators use command-line tools for managing the Neo4j DBMS. The main tools used that are part of the Neo4j
installation (located in the bin directory) include:

    neo4j used to start, stop and retrieve the status of the Neo4j DBMS instance.

    neo4j-admin used to create, copy, remove, backup, restore and perform other administrative tasks.

    cypher-shell used to create, start, stop, drop databases and indexes and to and retrieve data from a user database.



This includes ensuring that the Neo4j software is up-to-date and configured properly, monitoring the use of Neo4j,
performing life cycle activities such as backups, and managing multiple Neo4j instances


Applications connect to a Neo4j database using the binary Bolt protocol, which is supported by the official language drivers.


Many deployed applications use Causal Clustering. Causal Clustering is an architecture which provides data safety and performance for
critical systems that could be distributed all over the world.

Example architecture #1: Using Neo4j as the primary database

Example architecture #2: Integrating Neo4j with other databases

Neo4j is often used in combination with other data sources (polyglot persistence),
 such as relational or NoSQL databases. Data can be fetched from the different systems
 in order to be manipulated by an application and subsequently written back to Neo4j. 
 Other solutions can involve loading of data from a number of databases into Neo4j in
 order to efficiently do connected data analysis on the combined data set.


Microservices

With cloud-hosted microservices, the implementation chosen for the microservice is one that performs best for the type of transaction.
 In a cloud-based microservice architecture, there may be a number of services that Neo4j can provide to the end-users.



product catalog  - mongo 
recomondation    - neo4j
account          - mysql
order            - oracle


With Causal Clusters, you configure multiple Neo4j instances that
communicate with each other about updates to the database(s). Causal Clusters
are used when data needs to reside in multiple physical locations, or if you want
to implement a high availability architecture where access to data will not be affected
if a Neo4j instance goes down.

core instance read write 
replica - read..

https://neo4j.com/developer/guide-cloud-deployment/

The underlying OS for the Docker instance is Debian

21Neo4j Desktop includes Enterprise Edition so you can fully develop and test
an application prior to production. However, you cannot use Neo4j Desktop for production purposes. 


A Neo4j instance can support multiple active user databases

Multiple user databases and Causal Clustering are not available in Community Edition. 

Number and location of Core Servers and Read Replicas
Number and locations of data centers.

Many of the Neo4j 4.x features covered in this training such as multi-database instances and online backup
are only supported in Neo4j Enterprise Edition. 

load balancing is provided by Neo4j. It can also be configured to meet specific criteria.


Managing logging

Monitoring queries

There are many common administrative tasks for a server mode deployment:

    Managing a Neo4j database

    Managing plugins used with the Neo4j database

    Managing logging

    Monitoring queries

    Backing up and restoring databases

    Managing Causal Clusters

    Managing database security

    Importing data


8 tasks 

https://18.223.110.57/

 ec2-18-223-110-57.us-east-2.compute.amazonaws.com

https://ec2-18-223-110-57.us-east-2.compute.amazonaws.com:7473/browser/


5/4

 Cypher is already pretty good at deconstructing nested documents


5/5

In this ebook, data professionals will learn how
to apply Agile principles to the activities of data
ingestion, modeling, and transformation in order
to uphold rigorous governance, auditability, and
maintainability,

 Is there a way to simplify the overall
development/testing/production cycle for creating
data models, managing metadata, and enforcing good
governance? What is the best way to integrate many
different commercial and open source tools into the
processes you use to create data pipelines?

 you can apply Agile
principles to the activities of data ingestion, modeling,
and transformation

DataOps engineers
DEFINING TERMS
AND CONCEPTS
manage the flow of data. That includes automating,
monitoring, and auditing the processes that are required
to keep data current, accurate, and availab

Well-coordinated data management procedures enable
businesses to ensure data quality, track versions,
enforce data privacy regulations, and keep data
applications moving through a continuous cycle of
development, integration, testing, and production

DataOps helps data professionals model,
maintain, and share data by shortening the
feedback loop among developers, testers, and
other stakeholders


DataOps takes its cues from Agile
programming methods to ensure the delivery
of data via a continuous “plan–develop–
build–manage–test–release–deploy–operate–
monitor” loop.


which may need to be masked or
tokenized to uphold data privacy regulations.


Snowflake Zero-Copy Cloning 

Snowflake provides near-limitless compute and
storage resources for development

Snowflake’s inherent support for all types
of data simplifies data management.

Snowflake’s automatic schema detection
speeds up modeling and design work for data
formats that contain metadata. 

The Snowflake architecture is sometimes
described as infrastructure as code. That means
the entire configuration can be defined and
stored in a version control system. 


Snowflake simplifies pipeline development by allowing
data engineers to orchestrate multiple streams and
tasks. A stream is a special object type that uses CDC
technology to track the ongoing changes in a table,
including inserts, updates, and deletes as well as data
manipulation language (DML) changes. Streams allow
analytic apps to query just the incremental changes
rather than the entire data set. A task defines a
recurring schedule for executing SQL statements,

More pipeline observability and UX features are
also coming to Snowflake. For example, as data is
loaded into Snowflake, operations personnel can use
the Snowflake Ingestion Dashboard


Having access to that data and the ability to generate actionable
insights quickly is now an absolutely must

 simply making modi‐
fications to existing data platform architectures did not solve the scalability problem.

hree different
Snowflake architecture layers in detail: Cloud Services Layer, Query Processing (Vir‐
tual Warehouse) Compute Layer, and the Centralized (Hybrid-Columnar) Database
Storage Layer.


The shared-disk architecture was an early scaling approach designed to keep data
stored in a central storage location, 

While the approach is simple in theory, it requires complex on-disk locking mecha‐
nisms to ensure data consistency which, in turn, causes bottlenecks. 

The shared-nothing architecture, in which storage and compute is scaled together
(Figure 1-2 ), was designed in response to the bottleneck created by the shared-disk
architecture

hus, organizations often overprovision shared-
nothing resources, which results in unused, unneeded resources


the Snowflake team’s approach was to build an entirely new
modern data platform, just for the cloud, that allows multiple users to concurrently
share live data


The Snowflake hybrid-model architecture is comprised of three layers ( Figure 1-3 )
known as the cloud services layer, the compute layer, and the data storage layer. 

It also includes management functions for handling infrastructure
and metadata, as well as performing query parsing and optimization, among other
features.


A Snowflake compute cluster, most often referred to simply as a “virtual warehouse,”
is a dynamic cluster of compute resources consisting of CPU memory and temporary
storage

DML operations -data manipulation languages.

Snowflake’s unique architecture allows for separation of storage and compute which
means any virtual warehouse can access the same data as another, without any con‐
tention or impact on performance of the other warehouses


Commands for virtual warehouses can be executed in the Web UI or within a work‐
sheet by using SQL.

USE ROLE SYSADMIN;
ALTER WAREHOUSE WH_CH2
SET
WAREHOUSE_SIZE = LARGE


USE WAREHOUSE WH_CH2;

Snowflake’s centralized database storage layer holds all data, including structured and
semi-structured data. As data is loaded into Snowflake, it is optimally reorganized
into a compressed, columnar format and stored and maintained in Snowflake data‐
bases.

There are two unique features in the storage layer architecture – time travel and zero-
copy cloning

Snowflake’s data storage layer is sometimes referred to as the Remote Disk layer. The
underlying file system is implemented on Amazon, Microsoft, or Google Cloud
(Figure 1-19 )

 There is no additional storage charge until
changes are made to the cloned object because zero-copy data cloning is a metadata-
only operation

Time travel allows you to restore a previous version of a database, table, or schem

There are three Snowflake caching types; query results cache, vir‐
tual warehouse cache, and metadata cache


CREATE DATABASE is the command used to create a new database, clone an exist‐
ing database, create a database from a share provided by another Snowflake account,
or to create a replica of an existing primary database (i.e., a secondary database).


A SQL UDF evaluates SQL statements and it can refer to other UDFs, although a SQL
UDF cannot refer to itself either directly or indirectly.
A JavaScript UDF is useful for manipulating variant and JSON data. A JavaScript
UDF expression can refer to itself recursively, although it cannot refer to other UDFs.


05/09
        (indexes are not the foregin keys of rdbm)
more on index free adjacency.   Graph DB does it RDBM does not.
Imagine one table to hold both people and things, and another table to hold the relationships between them.
chase pointes and memory.

write queries are slower in graph db
read queries can be very slow in rdbm.

    Blob supports CRUD (create, read, update, delete) operations at the file level.
    The directory or file path is the index.

So you can quickly locate and read the file. But locating something within a file
requires a sequential scan. Documents, images, audio, and video files are stored
in blob

While transactions are on rows (records), analytics properties are computed on columns (attributes).
OLAP applications need an optimized column-read operation on a table.

Document Datastore

Document stores are for storing and retrieving a document consisting of nested objects. a tree
structure such as XML, JSON, and YAML.  the document stores exploit the tree structure of the value.

SQL vs. NoSQL Database Comparision

Non-relational NoSQL datastores gained popularity for two reasons:

    RDBMS did not scale horizontally for Big Data
    Not all data fits into strict RDBMS schema


modeling relationships in a graph more natural than
using pivot tables to model
many to many relationships - the query slows down as volumes increase.

(did stop using pivot tables with neo4j)  (slightly use them in spark).


https://www.youtube.com/watch?v=XX7Ppc5T0GE

a pair of round brackets ( ) used to mark off a parenthetical word 

() round brackets - parentheses
[] square brackets - brackets
{} curly brackets  - braces
<> angle brackets - cheverons 

the advantage of where clause is that logical expressions can be applied for query
    flexiblilty.

https://graphacademy.neo4j.com/courses/cypher-fundamentals/1-reading/7-c-filtering-queries/




5/10

a node property is a list.  suppose append the list.... an apoc for that???


schema constraint validation failed.


https://community.neo4j.com/t/understanding-how-merge-works/318

https://www.youtube.com/watch?v=ABXp12tsKj0


https://www.youtube.com/watch?v=TO00ELu_uJI




Modeling your domain
understanding your data/users
getting the most out of your time

graph data bases distinguish themselves through their 
ability to maintain arbitrary connections between data 
as a fully materialized identity that is as available 
for manipulation as any other piece of information.  (index free adjacency)

not a sterelized list of facts but a custodian of knowledge. 

entities collections...
objects references and collections....  represent complex domains
and in memory...

the object relational mismatch...


data model vs schema

dbms has data model as core atribute and always treats data accoding to the model.
schema on the other hand is a set of contraints put on top.
uniqueness, foreigen keys, table column names are schema structurs on top of data model.

logical schema - the actual set of values stored in the database.
rule schema - a formal set of rules that define what values are allowed to be stored in the 
database.

safe ,consistant acid complaint 
efficient access
scalability

relationships between nodes are fully materialized independent entities.

cypher has been designed with graph pattern matching as its primary goal.
job of exicuting query is handed off tho the query planner and optimizer.

the planner diagram
from query text to parcing   ... to logical plan to physical plan to results.

page cache which maintains file fragments in memory .
network layer for client to server.

the data volume resource utilization graph 

database normalization  
scale it by de normalize it.


schemaless because they may limit the results of the query.


want this watched:
https://www.youtube.com/watch?v=TmVlq33u6l4

worst practices:



5/11

https://neo4j.com/docs/cypher-manual/current/clauses/foreach/?_gl=1*e6rcnl*_ga*MTAwMjI3NDEwMC4xNjQ1NzEwNTgw*_ga_DL38Q8KGQC*MTY1MjI3NDUxNS42NS4xLjE2NTIyNzQ4NTUuMA..&_ga=2.116584390.1543982176.1652274519-1002274100.1645710580

The FOREACH clause can be used to update data, such as executing update commands on elements in a path,
or on a list created by aggregation

The variable context within the FOREACH parenthesis is separate from the one outside it



Poulomi Mitra Thakur
, Associate System Design Engineer at Tata Consultancy Services (2019-present)
Answered Jan 15, 2021

a RESTful API allows you to query your java app via HTTP or similar web protocol. JDBC is Java's way of interfacing with the database. You'd be
 using JDBC unless your database already has an API implemented.

Rest api is faster and also easier to implement .REST API code packs up the data into Json and sends it back .

There is no practical way to develop a browser-based application written in JavaScript with just JDBC.

In a RESTful Web service, requests made to a resource's URI elicit a response with a payload formatted in HTML, XML, JSON, or some other format.

document model a collection of atributes.

The REST architectural style  a layer of abstraction on the server by defining resources that encapsulate entities (e.g. files
can be a resource: an image, a database query, a temporal service (e.g. “today’s weather in London”), or even a collection of other resources

In computing, a stateless protocol is a communications protocol in which no session information is retained by the receiver, usually a server. 



https://www.youtube.com/watch?v=oMTmG4ClO5I
recomendation engines

collaborative filtering:  an algorithm that conciders users interactions with products
with the assumption that other users will behave in similar ways.

content based: an algorithm that considers similarities between products and categories of 
products.
 
eg
customer bought product 1,2
customer' bougth product 1,2,3


silos and polyglot persistence

products have 
inventory location category price configurations 

customers 
purchase return review, view instore pruchaces location

results in data stored in different types of datastores

product catalogue in a document store
cutomer purchace is in relatonal data base
views in wide column store
shopping cart in key value store  -  a data structure more commonly known today as a dictionary or hash table. 

instor purchase in differned relation database.

the data lake is non-operationa, slow queries,  good for analytics bi map reduce.

recommendations require an operation workload -  its in the moment, real time.
respective stores queried for relevant informamtion...


:play is a restfull endpoint????  (the neo4j browser)

:play http://guides.neo4j.com/sandbox/retail.recommendations

first silo: product silo
CALL apoc.load.jdbc("jdbc:mysql://localhost:3306/furniturestore?user=root","Select * from products") yield row
Merge...
.....
.....

//  the furnature store table a product catalogue simple one table catalogue.

recommend within the furnature store silo would involve similar products.
write simple cypher returning similar product return.
write simple cypher returning similar designer.
write a path through the parent category from desk to home office down the lamps,bookcases...


bring in information specific to user
now customer and transaction silo

Load csv with headers from "https://docs.google.como/spredsheets/u/0/d/....
merge customer ,city ,lives in ...

note first pulled data from jdbc sql query
     second pulled data from flatfile/csv/spreadsheet.

so, have integrated catalogue /  customer , customer transaction siloes

can review schema/meta model...in neo4j that is...

now collaborative filtering...
so who is buying the same things as person1
what are they buying,  recommend thes things to person 1
recomment the thing most bought by the other people.

have more information in graph

product review
information is in document database 
a json documnet in mongo/couchbase....

CALL apoc.load.json("http://guides.neo4j.com/sandbox/retail-recommendatons/dat/reviews.json") 
yield value as row
...

how does this information improve the collaborative filtering
prediction? things highly rated that were rated along with item
of interest...

now bring in inventory information.

note the store inventory relationship has the number avalable
property.

and how does this improve the recomendation
make sure the item recommended is in stock...
and is in same store where customer lives...

content based filtering vs collaborative filtering...

https://neo4j.com/docs/cypher-manual/current/clauses/with/
For convenience, the wildcard * expands to all variables that are currently in scope and carries them over to the next query part (see Using the wildcard to carry over variables).

https://data-xtractor.com/blog/databases/neo4j-cypher-hacks/

https://towardsdatascience.com/introduction-to-two-approaches-of-content-based-recommendation-system-fc797460c18c


https://neo4j.com/developer/neo4j-browser/

WARNING: A HTTP GET method, public javax.ws.rs.core.Response org.apache.zeppelin.rest.CredentialRestApi.getCredentials(java.lang.String)
throws java.io.IOException,java.lang.IllegalArgumentException, should not consume any entity.



5/12

Speaker: Karthik M Swamy (@krtk)

perscriptive

find best possible action for a given data
recommends an action
you want suggestions for possible actions
a recommender system

yea old descriptive statistics

predictive: statistical modelling of data, predictions about the future
info about future or data missing, sentiment analysis.

recommender:  match user to most relevant items.

suggest based on usage history
                 people like us.

how to recommend:

content based recommendation
   analyze content of item and user profile  -  text and grammar play a role.

and again from yesterday

collaborative filtering...

Best approach:  hybred of above...

content-based filtering:

yea old movie set

is science fiction
is same director
has super hero
is a thriller
same musician.


collaborative filtering
have friends and they like or not each in the set.
note:;  hitting like buttion contributes...

likeing everything though....  maybe noise no content...

challenge with collaborative filtering:  
 no users to start with.  (outlier)
 no similar profiles.     (outlier)
 similar profile, disparate interest.
 different users mixing same dataset.


Approach decomposition

have n users and their ratings.
m movies and their ratings.

predict rating given by user u and item i.

metric RMSE between true ratings and predictions.

Singular Value Decomposition (SVD), a classical method from linear algebra












there are few more approaches to build recommender systems such as multi-criteria
 recommender systems, risk-aware recommender systems, mobile recommender systems,
 and hybrid recommender systems (combining collaborative filtering and content-based
 filtering). 

https://analyticsindiamag.com/singular-value-decomposition-svd-application-recommender-system/

The Singular Value Decomposition (SVD), a method from linear algebra that has been generally
 used as a dimensionality reduction technique in machine learning. SVD is a matrix
 factorisation technique, which reduces the number of features of a dataset by reducing
 the space dimension from N-dimension to K-dimension (where K<N). In the context of the
 recommender system


why tensorflow?
factorization is a conventional approach 
  it discovers underlying latent features.
latent features are 'hidden' features to distinguish them from observed features.
 Latent features are computed from observed features using matrix factorization.
 An example would be text document analysis. 'words' extracted from the documents are features.
  
cluster commputable
gpu accelerants.

"The paradox of choice"

and have information overload...

have users and items...  
percieve search as interactions with both entities.

ckicking on cars and viewing ads
people are contacting dealers
people are rating purchaces

inherent interest is implicit preference
rate an item is explicit prefrence

and have an interaction matrix sparcly populated with views clicks and ratings.
and predict the empty matrix cells...

the approach to the recommendation...

continuous featurs.
categorical features.


technical preprocessing

data fetch
type conversions
user and item ID rehashing
user profile overlap
reduction to sample size

@JackTwain the correct PCA analogy is that the latent features are the eigenvectors.
 The principal components are the weights assigned to each observation for the principal
 eigenvectors. In other matrix factorisation models the latent features play the role of
 the eigenvectors. This may sound pedantic, but the mistake creates no end of confusion for
 people. – 
conjectures

tensorflow again a general mamthamatical toolkit.

 association rule learning.


data.gov


arrow tool alistair jones

maybe what is current date format for neo4j

Using LOAD CSV in the Real World | Nicole White, Data Scientist, Neo4j
https://www.youtube.com/watch?v=Eh_79goBRUk


the constraints as dilgience.
and cypher processing such as toUpper as ensuring uniqueness in merge...


create constraint(c:complaint) assert c.id is unique;
create constraint on (c:company) assert c.name is unique;

Load csv with headers from "file:///Users/nicole/consumer_complaints.csv' as line
maybe have to split(line.'Date received', '/') as date

and make sure indexs exist on properties being merged on...

The very specific use case for periodic commit...



maybe, back in 2015 use the shell when usning load csv....
because expressive errors....


note:  

first read of .csv did company complaint response....
there are other segments of the graph to consider....
will have to use the .csv for the next component of the graph....

remember say empty string as a property.  cannot set a property to null
so cypher will skip over them...  or rather manipulate the csv .
eg sub issues are usually blank....



https://www.youtube.com/watch?v=fC8HfepCDgE
Graph Representation Learning From Knowledge Graphs to Recommender Systems / Hongwei Wang (UIUC)

https://www.youtube.com/watch?v=8tv6_oedpjQ&t=106s
4. Authorising HTTP requests with Neo4j | Building Web Applications with Neo4j and Typescript

various data - each it's owne source for consuption


https://www.youtube.com/watch?v=veLqJzZBJEw
Graphs in Retail Know Your Customers and Make Your Recommendation Engine Learn

organisatonal data
product data
customer data
3rd party data
event data
supply chain data

the guy said: "and you can't do machine learning in real time."


seems to me the normalize de normalize data...  graphicly that is...



https://www.youtube.com/watch?v=jiE3wsrVUQs
Using Neo4j and Machine Learning to Create a Decision Engine — Tim Ward, CluedIn



https://www.youtube.com/watch?v=c9z_ICD44XQ

also reads the csv several time each time compleating the graph more.

these recommendations are based on a count score...

maybe have to worry about keyword extraction.

textblob

that is, extract keywords and add nodes with keword properties to the graph.
with keywords attached to events,  can query pairs of keywords acrooss events and see which events share same keywords.

that is, find clusters of topics...

modularity the ratio of number of relationships that go outside a cluster to the number of relationships staying inside the cluster


write the clusters back to the graph...  



recommendations have value
35 percent of amazon
70 percent of netflix...

have a user and item...

care about events,  and it tells of preferences...
implicit preference data   
   online page view, click, app interation
   commerce,   cat purchase return
    media preview watch listen
explicit preference data 
   ratings riviews
intent 
   search query
social 
   like share follow unfollow block

context...recommending the most relevant items to users without taking into account any ad-
ditional contextual information, such as time, location, or the company of other peo-
ple (e.g., for watching movies or dining out).

prediction....

deep learning as feature extraction


factorization machines... general factor models...

codait.org

https://www.youtube.com/watch?v=ZspR5PZemcs

prediction is ranking
given a user and context, rank the available items in order of likelihood that the user
will interact with them...


https://www.youtube.com/watch?v=QnozzFP_fPo


read query tuning chapter 6

query log analyzer

explain and profile

cypher-chell plans-inverted tree

Apache Lucene is a free and open-source search engine
software library, originally written in Java by Doug Cutting. I

properties are expensive to access, so wait till node is distinct.

https://www.youtube.com/watch?v=QnozzFP_fPo

index backed order by

mind the cardinality...

call apoc.meta.graph

https://www.youtube.com/watch?v=BN5T8IimB78


improve performance by removing lable checks. when only one labe exists...


the reason to run load csv multiple times is to avoid eagerness...

map objects....

https://neo4j.com/blog/join-context-neo4j/


post sort operations on unions 

unwinding an empty array terminates the query.
forEach on empty collection does nothing and query continues.

queries run on multiple threads.

versioning the data model.


7/15

looking at  

curl    the restfull endpoint  
cat     file.cypher | some program


As you get familiar with the MERGE statement, you may get the impression that it guarantees uniqueness
After that statement, you will still end up with multiple Johns in the database. If you really want uniqueness, use the UNIQUE constraint.

Also, when you run a cluster over subnet boundaries – especially if you cross multiple regions – it makes it easy for the NSA because the 
network traffic in the cluster is not encrypted, so everyone can read your data.

And don’t forget that your Garbage Collection (GC) pauses should never take longer than the cluster timeout.
What results is the lovely* (*sarcasm) effect of a round-robin master switch.


https://neo4j.com/docs/operations-manual/current/tutorial/local-causal-cluster/

Core instances do so by replicating all transactions using the Raft protocol.

https://neo4j.com/docs/operations-manual/current/clustering/introduction/

Neo4j supports seeding a cluster from a database dump, a database backup, or from another data source (with the Import tool)

If you have a running Neo4j database that you want to seed in a running cluster, use neo4j-admin backup to create a database backup.
 This could be a backup from a standalone Neo4j instance or another cluster member (e.g., an existing Read Replica).

https://neo4j.com/docs/operations-manual/current/clustering/internals/


Abstract: This workshop is a hands-on lab with an ambitious goal: show three practical 
examples of using ontologies in Neo4j, step by step, in a way that attendees can follow
 and understand or even join and code along (on their local Neo4j or in a Sandbox). 
The three examples are as follows. Data import: Use an ontology as the target model
 for the graph that your ETL pipeline will build in Neo4j. Model validation: Use an 
ontology to define constraints on the shape of your graph and create a data quality
 dashboard reporting on the violations of those constraints in your Neo4j graph. Semanti
c search: Use the ontology to semantically annotate a dataset and implement semantic
 search and semantic similarity using Cypher over your data and the ontology. For this 
workshop, we will use public ontologies in different domains (FIBO, Schema.org, ESCO

https://edmcouncil.org/page/financialindustrybusinessontology

algo.unionFind


https://neo4j.com/docs/driver-manual/4.0/session-api/simple/


https://books.google.com/books?id=pGYREAAAQBAJ&pg=PT145&lpg=PT145&dq=difference+between+neo4j+driver+and+jdbc&source=bl&ots=VkmzujGWsG&sig=ACfU3U3nuaH27Bn621eEnB2KubKdAsg9tQ&hl=en&sa=X&ved=2ahUKEwirsPH4qOn3AhVMpIkEHUbmDF0Q6AF6BAgkEAM#v=onepage&q=difference%20between%20neo4j%20driver%20and%20jdbc&f=false




https://www.brokerfp.com/User/SignUp

Appointment # 12767177 for Thomas Pries is scheduled for:
May 24, 2022 at 2:00 PM

Pestukas1!


Your Appointment Location
The UPS Store #3660
42 Lake Avenue Extension (ground floor of Maron Hotel & Suites)
Danbury, CT 06811


The performance of Cypher queries using ORDER BY on node properties can be influenced by the existence
 and use of an index for finding the nodes. If the index can provide the nodes in the order requested in
 the query, Cypher can avoid the use of an expensive Sort operation. Read more about this capability in
 The use of indexes.


note 20 cypher clauses exist...


:play displays html pages.

https://neo4j.com/docs/cypher-manual/current/syntax/expressions/

maybe 20 expression types....

the cypher with clause will not allow you to 
a chain query parts together, passing reuslts from one part to another
filter and limit the number of rows passed to the next part
perform intermediate processing during a query.


https://stackoverflow.com/questions/62179986/using-snowflake-with-neo4j


https://neo4j.com/labs/apoc/4.1/cypher-execution/parallel/


https://stackoverflow.com/questions/69247556/neo4j-query-using-only-one-core-of-my-machine

You can use CALL apoc.periodic.iterate() to do parallel executions, using the parallel: true parameter that you add to options.

However -- normally these are for write operations, and your query is a read. It's not clear what you want to do in terms of a parallel read here.


https://community.neo4j.com/t/how-best-to-do-parallel-processing/13342

graph modularity




    How Queries Work in Neo4j

    Preparing for Query Tuning

    Reducing Cardinality

    Optimizing Property Access

    Monitoring Queries


The physical execution plan runs in the appropriate runtime,
 which is based upon the operations in the execution plan. Some
 operations cannot run in parallel so this affects which runtime is used
 to execute the query. And finally the runtime returns the results to the client.


Query performance factors

There are other factors that affect the performance of a query:

    Version of Cypher you are using.

    Cypher runtime you are using. Note that the runtime used may depend on the operations used for a query.

    Cypher replanning settings.


That is, you must re-measure your query performance and possibly change queries with each version of Neo4j. 

Cypher runtime you are using. Note that the runtime used may depend on the operations used for a query.


MATCH (m:Movie)<--(p:Person)
WHERE p.name = $name
RETURN  m.title


Notice also that steps that incur db hits are highlighted in orange. Observing db hits is important as it may show areas of the query that could be improved.


https://neo4j.com/graphacademy/training-cqt-40/01-cqt-40-how-queries-work-in-neo4j/



PROFILE MATCH (m:Movie)
UNWIND m.genres AS name
WITH  name, collect(m) AS movies
MERGE (g:Genre {name:name})
WITH g, movies
UNWIND movies AS m
WITH g,m
MERGE (g)<-[:IS_GENRE]-(m)


MATCH (p:Person)
CREATE (p2:Person {name:p.name})


 Eager operations are explicitly used when 
the Cypher code includes keywords or function
s that require eager behavior during the
 query processing. Eager operations are 
implicitly used in execution plans to
 prevent infinite loops.

PROFILE
MATCH p = (a:Actor)-[:ACTED_IN]-(m:Movie)
WHERE 1942 <= m.releaseYear <= 1945
FOREACH (x IN nodes(p) | SET x.USWarTime = true)

In Neo4j 4.1, many performance improvements have
 been made to the Cypher runtime by implementing the
 PIPELINED runtime for many operators. Most read-only
 operators use PIPELINED runtime by default, but there
 are some that still use SLOTTED. Which runtime used by each
 operator is in the Cypher Reference Manual. Operators that
 modify the graph use SLOTTED runtime which is slower.

PROFILE
MATCH (m:Movie)
WHERE (:Person {name:$actor1})-[:DIRECTED]->(m)
OR (:Person {name:$actor2})-[:DIRECTED]->(m)
RETURN m.title

Here is the execution plan, where we see that SLOTTED is used because we require the LetSemiApply operator:

Rewrite query to use PIPELINED

Strive to eliminate SLOTTED from your (read-only) execution plans by rewriting the query to not use operators that must use SLOTTED. For example, this query can be rewritten to:
Cypher

PROFILE
MATCH (m:Movie)<-[:DIRECTED]-(p:Person)
WHERE p.name = $actor1 OR p.name = $actor2
RETURN m.title

Indexes are used only for the initial anchoring of the
 query (beginning MATCH pattern). You can use one or more
 indexes to anchor the query, but by default only one index
 is used.

5/23

https://neo4j.com/labs/neosemantics/4.2/introduction/
                      /neosemantics-rdf


store rdf data in Neo4j in a lossless manner
on demand export porperty graph data from noe4j as rdf
model mapping
model validation based on shacl shapes (constraints)  - nodes of these type connected to nodes of these type...
inferencing


pass the rdf as a paramater/ fetch froom url....


simple turtle filies...

http://neo4j.com/indiv#>

twopolynomial project has neosematics installed...


in neo4j browser...   :GET is an HTTP get request.


https://neo4j.com/blog/10-things-you-can-do-with-cypher/?mkt_tok=NzEwLVJSQy0zMzUAAAGEkH0ffayG5Mvaja8x-zV4MaaM-gabuy9Dye9qe9hRIGC4Igt9lhMPsogcROwpTnNTtQXNRHgQg-kHcaqpAao2BMAfGdMzKpS2ubVqRa6boOZx2Q

Neo4j Under The Hood – Graph Composition as a Recurring Pattern

https://www.youtube.com/watch?v=NEJ7lPVwJn8

customers, purchases are different graphs integrated by fabric.
the query, across the different graphs, because of fabric...

https://www.youtube.com/watch?v=OVweE--RJqM
2:36 / 40:19
How Semantic is Your Graph?

RDF semantics (1)    
vocabulary based Schema.org& GKG (google knowledge graph)

RDF semantics (2) 
Rule based (including OWL)

inferring new facts from existing ones
checking consistency of datasets...

publish lable property graph as rdf



https://neo4j.com/docs/cypher-manual/4.1/query-tuning/query-options/


Page Cache

The Page Cache is an in-memory copy of part or all of the graph. The area of memory used for Page Cache is managed by the DBMS, meaning that it is not automatically garbage collected in the JVM



https://neo4j.com/docs/cypher-manual/current/execution-plans/operator-summary/
https://neo4j.com/docs/cypher-manual/current/execution-plans/operators/#query-plan-apply


Goals for improving execution plans

As you gain experience with query tuning and viewing execution plans, your goals are:

    Avoid redundant work and operations.

    Early in the query, eliminate data that is going to be filtered out later in the execution.

    Recognize less expensive ways to do what you want:

        Improve the Cypher statement.

        Can you ensure query is using PIPELINED?

        Will APOC perform better for some processing?

        Will a stored procedure perform better?


CALL apoc.schema.assert({},{},true);

MATCH (n) DETACH DELETE n;

CREATE CONSTRAINT UniqueMovieIDConstraint ON (m:Movie)
ASSERT m.id IS UNIQUE;

CREATE CONSTRAINT UniquePersonIDConstraint ON (p:Person)
ASSERT p.id IS UNIQUE;

LOAD CSV WITH HEADERS FROM
'https://data.neo4j.com/advanced-cypher/movies2.csv' AS row
WITH row.movieId as movieId,
     row.title as title,
     row.genres as genres,
     toInteger(row.releaseYear) as releaseYear,
     toFloat(row.avgVote) as avgVote,
     collect({id: row.personId, name:row.name, born: toInteger(row.birthYear), died: toInteger(row.deathYear),personType: row.personType, roles: split(coalesce(row.characters,""),':')}) as people
MERGE (m:Movie {id:movieId})
  ON CREATE SET m.title=title, m.avgVote=avgVote,
  m.releaseYear=releaseYear, m.genres=split(genres,":")
WITH *
UNWIND people as person
MERGE (p:Person {id: person.id})
  ON CREATE SET p.name = person.name, p.born = person.born, p.died = person.died
WITH  m, person, p
CALL apoc.do.when(person.personType = 'ACTOR',
"MERGE (p)-[:ACTED_IN {roles: person.roles}]->(m)
           ON CREATE SET p:Actor",
"MERGE (p)-[:DIRECTED]->(m)
    ON CREATE SET p:Director",
{m:m, p:p, person:person}) YIELD value AS value
RETURN count(*);  // cannot end query with APOC call

CREATE INDEX PersonNameIndex FOR (p:Person) ON (p.name);

CREATE INDEX MovieTitleIndex FOR (m:Movie) ON (m.title);

CREATE CONSTRAINT UniqueGenreNameConstraint ON (g:Genre) ASSERT g.name IS UNIQUE;
MATCH (m:Movie)
UNWIND m.genres as name
WITH DISTINCT name, m
MERGE (g:Genre {name:name})
WITH g, m
MERGE (g)<-[:IS_GENRE]-(m)



MATCH (p:Person)-[:DIRECTED]->(m:Movie)
WITH count(*) as TotalDirectorRelationships
RETURN TotalDirectorRelationships
Change this query so that the count store will be used.



MATCH (a:Actor)
WHERE (a)-[:ACTED_IN]->(:Movie {title:$movie1})
OR (a)-[:ACTED_IN]->(:Movie {title:$movie2})
RETURN a.name

VS


PROFILE
MATCH (a:Person)-[:ACTED_IN]->(m:Movie)
WHERE m.title = $movie1 OR m.title = $movie2
RETURN  a.name


CALL apoc.schema.assert({},{},true);

MATCH (n) DETACH DELETE n;

CREATE CONSTRAINT UniqueMovieIDConstraint ON (m:Movie)
ASSERT m.id IS UNIQUE;

CREATE CONSTRAINT UniquePersonIDConstraint ON (p:Person)
ASSERT p.id IS UNIQUE;

LOAD CSV WITH HEADERS FROM
'https://data.neo4j.com/advanced-cypher/movies2.csv' AS row
WITH row.movieId as movieId,
     row.title as title,
     row.genres as genres,
     toInteger(row.releaseYear) as releaseYear,
     toFloat(row.avgVote) as avgVote,
     collect({id: row.personId, name:row.name, born: toInteger(row.birthYear), died: toInteger(row.deathYear),personType: row.personType, roles: split(coalesce(row.characters,""),':')}) as people
MERGE (m:Movie {id:movieId})
  ON CREATE SET m.title=title, m.avgVote=avgVote,
  m.releaseYear=releaseYear, m.genres=split(genres,":")
WITH *
UNWIND people as person
MERGE (p:Person {id: person.id})
  ON CREATE SET p.name = person.name, p.born = person.born, p.died = person.died
WITH  m, person, p
CALL apoc.do.when(person.personType = 'ACTOR',
"MERGE (p)-[:ACTED_IN {roles: person.roles}]->(m)
           ON CREATE SET p:Actor",
"MERGE (p)-[:DIRECTED]->(m)
    ON CREATE SET p:Director",
{m:m, p:p, person:person}) YIELD value AS value
RETURN count(*);  // cannot end query with APOC call

CREATE INDEX PersonNameIndex FOR (p:Person) ON (p.name);

CREATE INDEX MovieTitleIndex FOR (m:Movie) ON (m.title);

CREATE CONSTRAINT UniqueGenreNameConstraint ON (g:Genre) ASSERT g.name IS UNIQUE;
MATCH (m:Movie)
UNWIND m.genres as name
WITH DISTINCT name, m
MERGE (g:Genre {name:name})
WITH g, m
MERGE (g)<-[:IS_GENRE]-(m)



PROFILE
MATCH (a:Actor)-[:ACTED_IN]->(m:Movie)
WHERE $relYear1 <= m.releaseYear <= $relYear2 AND a.born > $yearBorn
RETURN a.name as actor, a.born as born,
collect(DISTINCT m.title) AS titles ORDER BY actor

PROFILE
MATCH (a:Actor)
WHERE a.born > $yearBorn
RETURN a.name AS actor,  a.born AS born,
[(a)-->(x) WHERE $relYear1 <= x.releaseYear <= $relYear2 | x.title] AS titles ORDER BY actor


The only type of query that supports case-insensitive queries is a full-text schema index.

CALL db.index.fulltext.createNodeIndex(
      'MovieTitleMixedCase',['Movie'], ['title'])

CALL db.index.fulltext.queryNodes('MovieTitleMixedCase', '*' + $testString + '*') YIELD node
WITH node AS movie
MATCH (a:Actor)-[:ACTED_IN]->(movie)
RETURN movie.title, collect(a.name) as Actors

MATCH path = (p1:Person)-[:ACTED_IN*6]-(p2:Person)
USING INDEX p1:Person(name)
USING INDEX p2:Person(name)
WHERE p1.name = $actor1
  AND p2.name = $actor2
RETURN [n IN nodes(path) | coalesce(n.title, n.name)]


CALL apoc.schema.assert({},{},true);

MATCH (n) DETACH DELETE n;

CREATE CONSTRAINT UniqueMovieIDConstraint ON (m:Movie)
ASSERT m.id IS UNIQUE;

CREATE CONSTRAINT UniquePersonIDConstraint ON (p:Person)
ASSERT p.id IS UNIQUE;

LOAD CSV WITH HEADERS FROM
'https://data.neo4j.com/advanced-cypher/movies2.csv' AS row
WITH row.movieId as movieId,
     row.title as title,
     row.genres as genres,
     toInteger(row.releaseYear) as releaseYear,
     toFloat(row.avgVote) as avgVote,
     collect({id: row.personId, name:row.name, born: toInteger(row.birthYear), died: toInteger(row.deathYear),personType: row.personType, roles: split(coalesce(row.characters,""),':')}) as people
MERGE (m:Movie {id:movieId})
  ON CREATE SET m.title=title, m.avgVote=avgVote,
  m.releaseYear=releaseYear, m.genres=split(genres,":")
WITH *
UNWIND people as person
MERGE (p:Person {id: person.id})
  ON CREATE SET p.name = person.name, p.born = person.born, p.died = person.died
WITH  m, person, p
CALL apoc.do.when(person.personType = 'ACTOR',
"MERGE (p)-[:ACTED_IN {roles: person.roles}]->(m)
           ON CREATE SET p:Actor",
"MERGE (p)-[:DIRECTED]->(m)
    ON CREATE SET p:Director",
{m:m, p:p, person:person}) YIELD value AS value
RETURN count(*);  // cannot end query with APOC call




explain MATCH (m:Movie)-[:ACTED_IN]-(a:Actor)
WHERE a.name = $actorName
WITH m
MATCH (m)-[:DIRECTED]-(d:Director)
RETURN collect(d.name) AS Directors


ZacharyPiperLLC


Form I-9 Paperwork:
 
If you did not complete your Form I-9 electronically via our digital process at your Fidelity Security fingerprint
 appointment, please email TAops@ftsjobs.com  to schedule your appointment within 3 days of your start date.

Did attend fingerprint security and present Ct Driver License and SS card.


UPS username user id
TomasJPries	
PriesTomas


Hi Thomas Pries,

This is one of two emails that you'll need to register for ADP Workforce Now so you can start using ADP Self Service.

After you enter your Personal Registration Code (provided in a separate email), select the Associate ID option and enter the Associate ID provided below.

Your Associate ID is: 6CZUR3170

Instructions:

1. Go to ADP Workforce Now

2. On the Login page, click Create Account.

3. Follow the instructions on the site.

4. After you register successfully, log in to ADP Workforce Now and complete your prehire tasks.

Have questions or need help? Contact your organization's administrator.


You have submitted Section 1 of your Form I-9. Your confirmation number is PRI1513156653
Remember to show your documentation to your manager or HR representative so they can verify your identity and work authorization.


must complete state tax witholding.

1410 spring hill rd tysons va







https://neo4j.com/graphacademy/training-cqt-40/02-cqt-40-preparing-for-query-tuning/

Preparing for Query Tuning
About this module

In this module you will learn about the steps you take before you begin your query tuning work.

At the end of this module, you will be able to:

    Ensure you have the appropriate system hardware and settings that can affect performance.

    Prepare Neo4j configuration settings for query tuning.

    Use a representative data set for your queries.

    Pre-test all queries to ensure expected rows based upon DB Stats.

    Ensure the Page Cache is warmed up.



System performance factors

The hardware and system settings required for executing Cypher queries in a Neo4j instance are the typical ones required by most applications:

    Disk - Ensure that you either use SSD or you have enough IOPs on the system.

        On linux, configure disk scheduler to noop or deadline, mount the database volume with noatime.

RAM - Ideally you want to fit your entire graph in memory


top and htop are Unix utilities that list information about processes and threads and how they are using resources like memory and CPU.

MATCH (n) RETURN max(id(n))
MATCH ()-[rel]->() RETURN max(id(rel))
// or
CALL apoc.warmup.run() //nodes and relationships
CALL apoc.warmup.run(true) // include properties
CALL apoc.warmup.run(true,true) // include large strings and arrays
CALL apoc.warmup.run(true,true,true) // include indexes


https://neo4j.com/graphacademy/training-cqt-40/03-cqt-40-reducing-cardinality/




How to reduce cardinality?

Here are some tips:

    Aggregate earlier where the grouping key will become distinct.

    Use pattern comprehension.

    Use subqueries (new in Neo4j 4.1)

    Use labels or indexes to select anchor nodes.

    Take advantage of indexes.

    WITH DISTINCT applies to the entire row, not just a single variable.

    LIMIT reduces all rows, not results per row.


PROFILE
MATCH (m:Movie {title: $movieTitle})
MATCH (dir:Person)-[:DIRECTED]->(m)
WITH m, collect(dir.name) AS Directors
MATCH (actor:Person)-[:ACTED_IN]->(m)
WITH m, Directors, collect(actor.name) AS Actors
RETURN m.title AS Title, Directors, Actors




PROFILE
MATCH (m:Movie {title: $movieTitle})
RETURN m.title,
[(dir:Person)-[:DIRECTED]->(m) | dir.name] AS Directors,
[(actor:Person)-[:ACTED_IN]->(m) | actor.name] AS Actors


CALL apoc.schema.nodes()


type='TOKEN LOOKUP'
type='UNIQUE BTREE'



PROFILE
MATCH (a)-[:ACTED_IN]->(m:Movie)<-[:DIRECTED]-(d)
USING JOIN ON m
RETURN collect(a.name), m.title, collect(d.name)

What happens here is that it does two expands to follow the path to m from a and from d. Then it compares the m’s from each side with each other in a hash-join. There are fewer rows in the execution plan, as well as db hits, and a lower execution time.

USING SCAN



PROFILE
MATCH (g:Genre)
USING SCAN g:Genre
WHERE g.name CONTAINS $text
RETURN g.name

Here is how the previous query could use the full-text schema index that you learned how to create
in the previous lesson on reducing cardinality.


Here is how the previous query could use the full-text schema index that you learned how to create in the previous lesson on reducing cardinality.


MATCH (m:Movie)
WHERE m.title = $movieTitle
RETURN m.title, m.releaseYear

When are the title and releaseYear properties accessed?

Select the correct answers.

    If there are no indexes on Movie.title or Movie.releaseYear, the Movie.title property is accessed during the query and the Movie.releaseYear property is accessed in the RETURN clause.

    If there is an index only on Movie.title, the Movie.releaseYear property is accessed in the RETURN clause.

    If there is an index only on Movie.releaseYear, the Movie.title property is accessed during the query and the Movie.releaseYear property is accessed in the RETURN clause.

    If there are indexes on both Movie.title and Movie.releaseYear, the properties are not accessed, but gotten only from the indexes.



1,4
1,2
1,2,4
1,2,3,4


If an index was used for the query, then that property value for that node will be cached and you need not go to the database to retrieve it.


A query may also take a long time to execute because of lock contention. This course does not cover multi-user query issues. 


A tool that helps you to understand your application queries is Neo4j Query Log Analyzer.

A tool that helps you to understand your application queries is Neo4j Query Log Analyzer.



prasadam mercy of god



Last day in may:


in rdf they call the property the relationship.

verticies are entities or literals.




grep
cat
curl  -"litterly pulled out anything related to music"




and agein rdf resource description framework...


sudo systemctl neo4j start


some notes on today zoom.

say the binary tree and want virtual relaton between leafs.
then shortest path between consecutive leafs.
with each pair of leafs virtual relation....












aws inovate 
tomaspries@gmail.com	  (maybec capital TOMAPRIES)
TheDayBeganWithRain1!



