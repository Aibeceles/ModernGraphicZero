10/30

The key to dealing with polling is: as soon as possible, get the direction of calls going from the point of state change to the observer. 
Then calls happen when they're needed. Not when they aren't. 

Functions that take other functions as parameters or that return functions as results are called higher order functions.

monoid extends semigroup[t]

file:///C:/Users/Aibes/ZeppelinDocs/Multi-Dimensional%20Aggregation%20%C2%B7%20The%20Internals%20of%20Spark%20SQL.html

case class Person(name: String,	age: Int)
val	getPerson:	ResultSetIO[Person] =		
 for {
 name <- get[String](1)
 age <- get[Int](2)
 } yield Person(name, age)

https://github.com/tpolecat/doobie


ScalaTestProject in Eclipse does return the requesite Vector.
In zeppelin it is to become a dataframe.

Do need a "Higher Level" loop/range.

within d* need rLow iterating to rhigh.

object Test {
 def main(args: Array[String]) {
 var a = 0;
 var b = 0;
 // for loop execution with a range
 for( a <- 1 to 3; b <- 1 to 3){
 println( "Value of a: " + a );
 println( "Value of b: " + b );
 }
 }
}

the makings of a dataframe:

QuerryS3T.main.scala creates row of vectors 
with each vector index representing a rowCounter value. 

Next up zepplin the vector into a dataframe.

QuerryS3T.main.scala needs the Case Class signature
as:  n:String, maxN:String,dim:String:rowResult:String

val x = (0 to 2).map { e => QuerryS3T.s3TQuerry(rLow:String,rHigh:String,nMax:String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(e).toInt)  }

for( a <- 2 to 4;b <- 0 to 2) {      //  a is dimension parameter as 2^a.  b is tConstraint parameter as 2^b

do not yet have n constraint

<console>:72: error: No TypeTag available for rResult
         val df = spark.createDataFrame(termsSeq)

11/1

yea old pivot table no more. spark pivot table forever forward.

11/2

case class rResult(n:String, maxN:String,dim:String,rowResult:String)

df
  .groupBy(grouping_columns)
  .pivot(pivot_column, [values]) 
  .agg(aggregate_expressions)


11/3

   build modules in eclipse with ant,maven,...

   run the main class of a java module 
   run the main class of a jar.

   jar a java module.
   standalone app a module.

from yesterday, df , per above, can not be reconstituted after z.put.  
it does exist as a ????

com.fasterxml.jackson.databind.JsonMappingException: Incompatible Jackson version: 2.10.3

looks like jackson databind in zadscripts need to be same version as zeppelin.

maybe need zadscriptsk cleaned of jackson jar.
maybe new project zadscriptskZ and no jackson dependencies.

maybe test the theory with manual dataframe for groupby, count table.

11/4

file:///C:/Users/Aibes/ZeppelinDocs/sql%20-%20How%20to%20select%20the%20first%20row%20of%20each%20group%20-%20Stack%20Overflow.html

C:\Users\Aibes\ZeppelinDocs\Spark Datasets vs DataFrames vs RDDs by Upendra Nallabolu Analytics Vidhya Medium.htm

And so, have abandoned .show a dataframe.  
Proceed with selecting from the dataframe, say a column.

scala value types
      smart constructors

typesafe.akka

refined types

the new kingmakers

spark
zip repartition pipe.

rdd's how to... not what to

.pivot needs a compatable jackson version.

 <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.4.4</version>
   </dependency>

jackson databind is in neo4j streams 4... maybe the offending version

zeppelin 0.8.2  

11/5

Paxdiablo's definition is really good.

In addition, please consider delivering an uber-jar is sometimes quite useful,
if you really want to distribute a software and don't want customer to download dependencies by themselves.
As a draw back, if their own policy don't allow usage of some library, or if they have to bind some extra-components (slf4j, system compliant libs, arch specialiez libs, ...) this will probably increase difficulties for them.

You can perform that :

    basically with maven-assembly-plugin
    a bit more further with maven-shade-plugin

A cleaner solution is to provide their library separately;
maven-shade-plugin has preconfigured descriptor for that. This is not more complicated to do (with maven and its plugin).

Finally, a really good solution is to use an OSGI Bundle. There is plenty of good tutorials on that :)

file:///C:/Users/Aibes/ZeppelinDocs/Maven%20Repository%20org.apache.zeppelin%20%C2%BB%20spark-interpreter%20%C2%BB%200.8.2.html

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>2.8.11</version>
        </dependency>
    </dependencies>

Typically these errors happen because your application is not bundled correctly.
When I'm working on something in Maven or SBT is usually analyze the output of jar tf jar-file which prints 
the classes in a jar. – Myles Baker Jan 10 '17 at 0:03

libraryDependencies ++= Seq(

https://cloud.google.com/dataproc/docs/guides/manage-spark-dependencies

URLClassLoader c1 = new URLClassLoader(new Url[] { new URL("file:lib/fatJarDep.jar"});

and create a factory that will instantiate classes of this library using this isolated classloader

Class.forName("className", true, c1);
Unshaded. 
Con: Files present in multiple JAR files with the same path (e.g., META-INF/services/javax.script.ScriptEngineFactory) will overwrite one another, resulting in faulty behavior.

https://getpocket.com/redirect?url=https%3A%2F%2Fqz.com%2F1284072%2Fthe-right-exercise-to-slow-down-heart-disease-according-to-a-new-study%2F&h=9ce884bb7efed901f0f64b5f050d040f43d05ba9b021d52cd5cede772a8d3f31&nt=0

C:\Users\Aibes\JDependencies\neo4j-jdbc-driver-4.0.0.jar

C:\Users\Aibes\JavaProjects\ZADScriptsK\ZADScripts\dist\ZADScripts.jar

zeppelin spark interpreter exclusions.

The class has a type parameter T. The + in front of this type parameter specifies that lists are covariant, as discussed in Chapter 19. 

case object Nil extends List[Nothing]

11/6

Have zeppelin 0.9.0 with is shadowinging dependencies but not on windows.
Have ZadScriptsK with NeoStreams dependency with transitive jackson dependency conflicting with Zeppelin 0.8.2 scala 
transitive jackson dependency.  

So, the exercise is:
Build the ZadScripts uber jar as a Maven project in Eclipse with shaded dependencies.

com.fasterxml.jackson.datatype/jackson-datatype-

No compiler is provided in this environment. Perhaps you are running on a JRE rather than a JDK? 

11/8

So, the exercise is:
Build/Rebuild ZadScriptsK as Maven uber jar in Eclipse with shaded dependencies.

But first, some command line maven???
           get the maven organized???
11/11

https://serverfault.com/questions/1040871/is-it-safe-to-mount-the-same-partition-to-multiple-vms

        <exclusions>
            <exclusion>
                <groupId>org.apache.zookeeper</groupId>
                <artifactId>zookeeper</artifactId>
            </exclusion>
            <exclusion>
                <groupId>log4j</groupId>
                <artifactId>log4j</artifactId>
            </exclusion>
        </exclusions>

vs 

<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-shade-plugin</artifactId>
  <version>3.0.0</version>
  <executions>
    <execution>
      <phase>package</phase>
      <goals>
        <goal>shade</goal>
      </goals>
      <configuration>
        <relocations>
          <relocation>
            <pattern>org.apache.commons</pattern>
            <shadedPattern>com.acme.shaded.apachecommons</shadedPattern>
          </relocation>
        </relocations>
      </configuration>
    </execution>
  </executions>
</plugin>

    Set the JAVA_HOME environment variable to point to a valid Java SDK (e.g. Java 8).
    Download and unzip Maven.
    Set the M2_HOME environment variable to point to the directory you unzipped Maven to.
    Set the M2 environment variable to point to M2_HOME/bin (%M2_HOME%\bin on Windows, $M2_HOME/bin on unix).
    Add M2 to the PATH environment variable (%M2% on Windows, $M2 on unix).
    Open a command prompt and type 'mvn -version' (without quotes) and press enter.

Super POM and POM inheritance.

The total POM file (result of all inheritance) is called the effective POM.

in the central Maven repository...  can i publish to the cmr???

 You configure an external dependency like this:

<dependency>
  <groupId>mydependency</groupId>
  <artifactId>mydependency</artifactId>
  <scope>system</scope>
  <version>1.0</version>
  <systemPath>${basedir}\war\WEB-INF\lib\mydependency.jar</systemPath>
</dependency>

The groupId and artifactId are both set to the name of the dependency. 
The name of the API used, that is. The scope element value is set to system.
The systemPath element is set to point to the location of the JAR file containing the dependency.
The ${basedir} points to the directory where the POM is located.
The rest of the path is relative from that directory. 

eclipse maven settings xml

You can execute either a whole build life cycle like clean or site, a build phase like install 
which is part of the default build life cycle, or a build goal like dependency:copy-dependencies.
Note: You cannot execute the default life cycle directly. You have to specify a build phase or
goal inside the default life cycle. 

Do want to know about pom exclusions tag.
Firstly though,
Scriptlets to emulate (two of them):
And deffering the scala topics...

Looks like dependencies top of pom.

<dependency>
  <groupId>mydependency</groupId>
  <artifactId>mydependency</artifactId>
  <scope>system</scope>
  <version>1.0</version>
  <systemPath>${basedir}\war\WEB-INF\lib\mydependency.jar</systemPath>     // maybe old old not working school.
</dependency>


<pluginManagement>   // possibly needed tag...
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-shade-plugin</artifactId>
  <version>3.0.0</version>
  <executions>
    <execution>
      <phase>package</phase>
      <goals>
        <goal>shade</goal>
      </goals>
      <configuration>
        <relocations>
          <relocation>
            <pattern>org.apache.commons</pattern>
            <shadedPattern>com.acme.shaded.apachecommons</shadedPattern>
          </relocation>
        </relocations>
      </configuration>
    </execution>
  </executions>
</plugin>

</pluginManagement>    // closure of possibly needed tag.

// relocation tags and transformer tags
//file:///maven/create-a-fat-jar-file-maven-shade-plugin/?utm_source=self&utm_medium=referral&utm_campaign=afterpost-related&utm_content=link1

// example properties - used with ${property-name} notation
	<properties>
		<jdk.version>1.7</jdk.version>
		<jodatime.version>2.5</jodatime.version>
		<junit.version>4.11</junit.version>
	</properties>

The maven shade plugin is beyond maven assembly plugin and is capable to do more things than the maven assembly plugin. The maven shade plugin has one goal defined:
shade: Invoked during the package phase


file:///C:/Users/Aibes/MavenDocs/Maven%20Shade%20Plugin%20Example%20Examples%20Java%20Code%20Geeks%20-%202020.html
//yummy.

and tell more about apache ivy....

You can use the Maven POM editor to have a look at the dependency hierarchy tree. 
Maven has several features to control the transitive dependencies.

To manage your transitive dependency exclusions you can use the dependency hierarchy tree. 
You can exclude any transitive dependency artifact using the exclude option in the POM editor.

https://stackoverflow.com/q/48665906/1744774    //  don't forget to look at this.

file:///C:/Users/Aibes/MavenDocs/java%20-%20How%20to%20add%20local%20jar%20files%20to%20a%20Maven%20project%20-%20Stack%20Overflow.html

file:///C:/Users/Aibes/MavenDocs/java%20-%20How%20to%20add%20local%20jar%20files%20to%20a%20Maven%20project%20-%20Stack%20Overflow.html


11/13

Exactly how you specify the Java compiler version depends on whether you are using Java 8 or earlier, or Java 9 or later.
From Java 9 Java got modules which complicates the compilation somewhat in the short run, but should help in the long run. 


<project>
  ...
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>2.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <relocations>
                <relocation>
                  <pattern>org.codehaus.plexus.util</pattern>
                  <shadedPattern>org.shaded.plexus.util</shadedPattern>
                  <excludes>
                    <exclude>org.codehaus.plexus.util.xml.Xpp3Dom</exclude>
                    <exclude>org.codehaus.plexus.util.xml.pull.*</exclude>
                  </excludes>
                </relocation>
              </relocations>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  ...
</project>


<project>
  ...
  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>2.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <relocations>
                <relocation>
                  <pattern>org.neo4j</pattern>
                  <shadedPattern>aibes.org.neo4j</shadedPattern>
                </relocation>
              </relocations>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>
  ...
</project>

C:\Users\Aibes\JavaProjects\ZADScriptsKEclipse\ZADScripts\target\ZADScripts-0.0.1-SNAPSHOT.jar

C:\Users\Aibes\JavaProjects\ZADScriptsKEclipse

shade disaster.....

retry from netbeans .jar

Annotations are computer processable, so you can use them to effectively add an extension to Scala

parabla with irrational vertex.

You only need to define two methods that are abstract in the trait: length and apply.


https://iopscience.iop.org/article/10.1088/0305-4608/5/5/017

file:///C:/Users/Aibes/ZeppelinDocs/Higher-order%20Functions%20Tour%20of%20Scala%20Scala%20Documentation.html

eclipse message implicit conversion found... scala runtime richint

https://getpocket.com/explore/item/why-it-pays-to-be-grumpy-and-bad-tempered?utm_source=pocket-newtab

11/16

Revisit the "working" but conflicting with dataframe.

ZADScripts.jar - ant does communicate with ChiefQuippy.  Though jackson does conflict with ZeppelinSpark.

And the Zeppelin local repo --- A Spark restart interpreter action after dependency artifact editing...
                                clears repo and resets??? ignores non-actual/current artifacts???

Did remove the neo4j.jdbc from spark dependencies.  It returned a null pointer exception.
The hypothesis is the dependency is where in some zeppelin lib folder.
The relevant lib is now in local repo. And...
Error:
org.apache.spark.SparkException: Added file file:/C:/Users/Aibes/zeppelin-0.8.2-bin-all/local-repo/spark/lib is a directory and recursive is not turned on

did subsequently remove lib and dependencies are all in local-repo and..  neo4j.jdbc not recognized.

So...  restart interpreter with explicit artifact dependency and...  lie..
add dependencies in local-repo and...  interpreter does not locate them.

Did copy and past dependencies into local repo.  restart interpreter.
Only registered artifacts remain in repo is current hypothesis...
Will add neo4j.jdbc as artifact dependency and...  back to the same problem...

Note:  often enough need windows restart as part of the setup.

The top level error:
com.fasterxml.jackson.databind.JsonMappingException: Incompatible Jackson version: 2.10.3

All the above was with ZADScriptsK -  ant.
Next experiment with ZADScriptsK - maven.
only ZADScripts maven in repo and no good.

next up... edit dfscripts and reflect on shaded classpath.

Looks likes much of org.neo4j is not in the shaded jar...  is that so???

Maybe the maven neo4j artifact at 117 kbits is something less that the whole of its parts...
it said it is the common parts for a variety of builds...

the ant neo4j is megabytes big. 

that is cutting to the chase.  

need the biggie in maven.

11/17 


terms is an arraybuffer of rResult, a case class.  It has the field rowResult a String representing BigDecimal.

//case class rResult(n:String, maxN:String,dim:String,rowResult:String) 
rResult need a method returning rowResult as long.

One way is to upload it to your own Maven repository manager (such as Nexus).


mvn org.apache.maven.plugins:maven-install-plugin:2.5.2:install-file -Dfile=<path-to-file>

mvn org.apache.maven.plugins:maven-install-plugin:2.5.2:install-file -Dfile=C:\Users\Aibes\JDependencies\neo4j-jdbc-driver-4.0.0.jar

Need web for this command

Question:

   IS zeppelin interpreter exceptions performant with regard to ZADScriptsK - ant?

   Can the pivot be performed with a python paragraph???   ---  Is the evening lab.

   and also, some %md commentary...   It's clear enough but need QuippyDataFrame clarity.

//  val df1= df.groupBy("n").count()
//  val df1 = df.groupBy("n").pivot("dim").count()
//  df2.show
//  df1.createOrReplaceTempView("tableName")

 Imagine trying to implement this same functionality using the visitor design pattern![2] Almost as awkward, imagine implementing it as a long sequence of if statements, type tests, and type casts.


11/18

file:///C:/Users/Aibes/ZeppelinDocs/Case%20Classes%20and%20Pattern%20Matching.html

fern desmond

case class Test(number: BigDecimal)

object Test {
    def apply(n) = new Test(n, new java.math.MathContext(2))
} 

I didn't like that one because it can be circumnavigated with the new keyword.

difference between match and case

did switch to fresh version of zeppelin 0.8.2.
did include ZADScriptsk and Neo4jStreams in interpreter.ff
and in sparktest get java.lang.NoClassDefFoundError: Could not initialize class org.apache.spark.rdd.RDDOperationScope$
and in QuippyDataFrame get com.fasterxml.jackson.databind.JsonMappingException: Incompatible Jackson version: 2.10.3

So do need shade lab.  
it includes installing neo4j-jdbc in maven repository for pom to work.

11/19

object ComprehensionTest3 extends Application

def foo(n: Int, v: Int): Iterator[Pair[Int, Int]] =      //it returns an iterator.

All given
constructor arguments a1, .., an must be constant expressions (i.e., expressions
on numeral literals, strings, class literals, Java enumerations and one-dimensional
arrays of them)

The work: QuippyDataFrame note page.

Has dependency problem. Maven lab not scheduled for today.
Do want to proceed with pivot. Alternative is 
.csv into dataframe.  The alternative implies ZADScrips 
execution from .cmd.

Have two flavors of ZADScripts.  Will file equip the ant version
and run from %cmd paragraph.

DFScripts_1.s3t(_):  . resultSet does again populate l WriteCSV.writLine list argument
                     . Is aware of WriteCSV with wCsv name. Though is null and needs
                       assignemnt in constructor. 


11/20

type Node <: NodeIntf


Unapply methods
Scala allows the definition of customized patterns:

val set = new scala.collection.mutable.HashSet[Any]
set += "This is a string" // add a string
set += 732 // add a number
set += ’c’ // add a character
set += true // add a boolean value
set += main // add the main function


trait Similar {
def isSimilar(x: Any): Boolean
}
case class MyInt(x: Int) extends Similar {
def isSimilar(m: Any): Boolean =
m.isInstanceOf[MyInt] &&
m.asInstanceOf[MyInt].x == x
}
object UpperBoundTest extends Application {
def findSimilar[T <: Similar](e: T, xs: List[T]): Boolean =
if (xs.isEmpty) false
else if (e.isSimilar(xs.head)) true
else findSimilar[T](e, xs.tail)
val list: List[MyInt] = List(MyInt(1), MyInt(2), MyInt(3))
Console.println(findSimilar[MyInt](MyInt(4), list))
Console.println(findSimilar[MyInt](MyInt(2), list))
}


class Stack[+A] {
def push[B >: A](elem: B): Stack[B] = new Stack[B] {
override def top: B = elem
override def pop: Stack[B] = Stack.this
override def toString() = elem.toString() + " " +
Stack.this.toString()
}
def top: A = error("no element on stack")
def pop: Stack[A] = error("no element on stack")
override def toString() = ""
}
object VariancesTest extends Application {
var s: Stack[Any] = new Stack().push("hello");
s = s.push(new Object())
s = s.push(7)
Console.println(s)
}
The annotation +T declares type T to be used only in covariant positions. Similarly,
-T would declare T to be used only in contravariant positions.

11/23

However, it augments that class with
well over a hundred operations in the StringOps class. F

Scala methods without parameters often don’t use parentheses. For example, the
API of the StringOps class shows a method distinct, without (), to get the distinct
letters in a string. You call it as
"Hello".distinct 

blocks are sequence of expressions  -  a statement is an anonomous class???

10. Write a function that computes x
n
, where n is an integer. Use the following
recursive definition:
• x
n
 = y
2
 if n is even and positive, where y = x
n / 2
.
• x
n
 = x· x
n – 1 if n is odd and positive.
• x
0
 = 1.
• x
n
 = 1 / x
–n
 if n is negative.
Don’t use a return statement.


In this chapter, you will learn how to work with arrays in Scala. Java and C++
programmers usually choose an array or its close relation (such as array lists or
vectors) when they need to collect a bunch of elements. In Scala, there are other
choices (see Chapter 13),

neo4j-jdbc is in .m2 directory.
Mayby last friday afternoon mvn install downloaded plugin worked.

It means can try shading again.

First off though, did build ZADScripts with shade.
Test QuippysDataFrame with spark interpreter set to include only ZADScripts dependency.

And the path to the jar is...


C:\Users\Aibes\.m2\repository\ZADScripts\ZADScripts\0.0.1-SNAPSHOT\ZADScripts-0.0.1-SNAPSHOT.jar

mvn deploy:deploy-file -Dfile=<path-to-file> -DgroupId=<group-id> 

mvn deploy:deploy-file -Dfile=C:\Users\Aibes\JavaProjects\ZADScriptsKEclipse\ZADScripts\src\lib -DgroupId=org

 

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>ZADScripts</groupId>
  <artifactId>ZADScripts</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>ZADScripts</name>
  <description>BuildAndQuerryQuippyDB</description>
  
 <repositories>
  <repository>
    <id>project-local-repo</id>
    <url>file://C:\Users\Aibes\JavaProjects\ZADScriptsKEclipse\ZADScripts\src\lib</url>
  </repository>
</repositories> 
  
  
  <dependencies>
  
  
  
  <dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
    <version>2.1.4</version>
  </dependency>
  <dependency>
    <groupId>log4j</groupId>
    <artifactId>log4j</artifactId>
    <version>1.2.17</version>
  </dependency>
  <dependency>
    <groupId>org.neo4j</groupId>
    <artifactId>neo4j-jdbc</artifactId>
    <version>4.0.0</version>
  </dependency>
  <dependency>
    <groupId>org.apache.kafka</groupId>
    <artifactId>kafka-clients</artifactId>
    <version>2.5.0</version>
  </dependency>
 
  </dependencies>
  


  
  <build>
    <sourceDirectory>src</sourceDirectory>
    <plugins>

      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
         </execution>
        </executions>
      </plugin>

    </plugins>
  </build>
</project>


  <dependency>
    <groupId>org.neo4j</groupId>
    <artifactId>neo4j-jdbc</artifactId>
    <version>4.0.0</version>
  </dependency>

Very promising and ready for zeppelin quippysDataframe:

mvn install:install-file -Dfile=C:\Users\Aibes\JDependencies\neo4j-jdbc-driver-4.0.0.jar -DgroupId=morg.neo4j-jdbc -DartifactId=neo4j-jdbc -Dversion=4.0.0 -Dpackaging=jar



mvn org.apache.maven.plugins:maven-install-plugin:2.5.2:install-file -Dfile=C:\Users\Aibes\JDependencies\neo4j-jdbc-driver-4.0.0.jar



[INFO] Installing C:\Users\Aibes\JavaProjects\ZADScriptsKEclipse\ZADScripts\target\ZADScripts-0.0.1-SNAPSHOT.jar to C:\Users\Aibes\.m2\repository\ZADScripts\ZADScripts\0.0.1-SNAPSHOT\ZADScripts-0.0.1-SNAPSHOT.jar
[INFO] Installing C:\Users\Aibes\JavaProjects\ZADScriptsKEclipse\ZADScripts\dependency-reduced-pom.xml to C:\Users\Aibes\.m2\repository\ZADScripts\ZADScripts\0.0.1-SNAPSHOT\ZADScripts-0.0.1-SNAPSHOT.pom
[INFO] 


did simply delete fasterxml in jar and did use that dependency in zeppelin.
The pivot does work.   would like to maven the solution... one of these days....

remember\ the pivot is by dimension.

DataFrames Versus Datasets
and
UDFs — User-Defined Functions


quippysDataframe mabe has a pivot to work with. Though it does used fixed precision.  

11/24


At the core of every Spark application is the Spark driver program, which creates a
SparkSession object.   --  And Zeppelin

Do want spark api for eclipse.

p.181

Encoders and... DecimalType/BigDecimal

// In Scala
import scala.util.Random._
// Our case class for the Dataset
case class Usage(uid:Int, uname:String, usage: Int)
val r = new scala.util.Random(42)
// Create 1000 instances of scala Usage class
// This generates data on the fly
val data = for (i <- 0 to 1000)
yield (Usage(i, "user-" + r.alphanumeric.take(5).mkString(""), r.nextInt(1000)))
// Create a dataset of Usage typed data
val dsUsage = spark.createDataset(data)
dsUsage.show(10)

There are a few things to observe about using higher-order functions and Datasets:
1. We are using typed JVM objects as arguments to functions.
2. We are using dot notation (from object-oriented programming) to access indi‐
vidual fields within the typed JVM object, making it easier to read.
3. Some of our functions and lambda signatures can be type-safe, ensuring compiletime error detection and instructing Spark what data types to work on, what
operations to perform, etc.
4. Our code is readable, expressive, and concise, using Java or Scala language fea‐
tures in lambda expressions.
5. Spark provides the equivalent of map() and filter() without higher-order func‐
tional constructs in both Java and Scala, so you are not forced to use functional
programming with Datasets or DataFrames. Instead, you can simply use condi‐
tional DSL operators or SQL expressions: for example, dsUsage.filter("usage
> 900") or d

sUsage($"usage" > 900). (For more on this, see “Costs of Using
Datasets”.)

For Datasets we use encoders, a mechanism to efficiently convert data between
JVM and Spark’s internal binary format for its data types (more on that in “Data‐
set Encoders”).

Dataset[T], where T is your typed object in Scala

Converting DataFrames to Datasets
For strong type checking of queries and constructs, you can convert DataFrames to
Datasets. To convert an existing DataFrame df to a Dataset of type SomeCaseClass,
simply use the df.as[SomeCaseClass] notation. We saw an example of this earlier:
// In Scala
val bloggersDS = spark
 .read
 .format("json")
 .option("path", "/data/bloggers/bloggers.json")
 .load()
 .as[Bloggers]
spark.read.format("json") returns a DataFrame<Row>, which in Scala is a type alias
for Dataset[Row]. Using .as[Bloggers] instructs Spark to use encoders, discussed
later in this chapter, to serialize/deserialize objects from Spark’s internal memory rep‐
resentation to JVM Bloggers objects.

One strategy to mitigate excessive serialization and deserialization is to use DSL
expressions in your queries and avoid excessive use of lambdas as anonymous func‐
tions as arguments to higher-order functions. Because lambdas are anonymous and
opaque to the Catalyst optimizer until runtime, when you use them it cannot effi‐
ciently discern what you’re doing (you’re not telling Spark what to do) and thus can‐
not optimize your queries (see “The Catalyst Optimizer” in Chapter 3).
The second strategy is to chain your queries together in such a way that serialization
and deserialization is minimized. Chaining queries together is a common practice in
Spark.

personDS
 // everyone above 40: lambda-1
 .filter(x => x.birthDate.split("-")(0).toInt > earliestYear)
 // everyone earning more than 80K
 .filter($"salary" > 80000)
 // last name starts with J: lambda-2
 .filter(x => x.lastName.startsWith("J"))
 // first name starts with D
 .filter($"firstName".startsWith("D"))
 .count()
As you can observe in Figure 6-3, each time we move from lambda to DSL (fil
ter($"salary" > 8000)) we incur the cost of serializing and deserializing the Person
JVM object.
Figure 6-3. An inefficient way to chain queries with lambdas and DSL
168 | Chapter 6: Spark SQL and Datasets
By contrast, the following query uses only DSL and no lambdas. As a result, it’s much
more efficient—no serialization/deserialization is required for the entire composed
and chained query:
personDS
 .filter(year($"birthDate") > earliestYear) // everyone above 40
 .filter($"salary" > 80000) // everyone earning more than 80K
 .filter($"lastName".startsWith("J")) // last name starts with J
 .filter($"firstName".startsWith("D")) // first name starts with D
 .count()


PM -  things to do with quippysDataFrame.
'
QuerryS3T - the extract and transform gateway from Quippy10 works with the ZADScripts dependency.

. need the spark sql stuff in another dataframe.
. need the pivot table description
  Is group by n,rowcounter, pivot by dimensions  -  how to multiple group.
- need print garbage out of ZADScripts.

Some paragraphs 

-  the convergance of vertexs within same index.
-  the generator and the constant term...  
   2,4,8 / 10,100,1000
-  make zeros worksheet
   . choose index, degree_1, index, degree_2 both = n, subtract and zero appears
   . repeate with index'  then multiple together and two roots....

And ZADScripts.dfScripts needs the S* cypher ruberic.

11/25

https://www.facebook.com/friends/?profile_id=100005664526904

https://aip.scitation.org/doi/abs/10.1063/1.5044557

11/27

Type inference could be much better if there were no implicit conversions.
 Better means: faster, more powerful, and more predictable.

Algebraic data types are really really important. The standard library is chock full of them (Option, List, and Try are some examples) 

Note that we have to use variance annotations here;

https://phys.org/news/2016-04-zipf-law-galaxies.html

So that could be done with extension methods

Implicit constructors, where we do not want any extension methods. For these,
I place the implicits in the companion object. This is a common pattern throughout ~all
my libraries (fastparse, os-lib, requests-scala, ammonite, mill, sourcecode, …)

Another option is we simply remove implicit conversions, and tell people who want both
an implicit constructor and a set of extension methods to define both explicitly. 

But if type inference really gets noticeably better without implicit conversions people
will have an incentive to stop doing that.



    An implicit conversion is applied automatically by the compiler in three situations:

        If an expression e has type T , and T does not conform to the expression’s expected type S .
        In a selection e.m with e of type T , but T defines no member m .
        In an application e.m(args) with e of type T , if T does define some member(s) named m , but none of these members can be applied to the arguments args .


Is there a solution yet for type parameters on generic extensions yet?

I currently use implicit conversions as a workaround for scala’s eager widening
in covariant types (so I use invariance with implicit conversion for widening).
Assuming we can fix that, I see no problem in getting rid of them.


This can be cumbersome when doing iterative development

11/29

import scala.language.higherKinds


file:///C:/Users/Aibes/ZeppelinDocs/Type%20Level%20Programming%20in%20Scala%20step%20by%20step%20-%20Dependent%20Types.html
file:///C:/Users/Aibes/ZeppelinDocs/Type%20Level%20Programming%20in%20Scala%20step%20by%20step%20-%20Implicits%20Resolution.html

http://danielwestheide.com/blog/2013/02/06/the-neophytes-guide-to-scala-part-12-type-classes.html

http://blog.rafaelferreira.net/2008/07/type-safe-builder-pattern-in-scala.html

http://danielwestheide.com/blog/2015/06/28/put-your-writes-where-your-master-is-compile-time-restriction-of-slick-effect-types.html

http://www.cakesolutions.net/teamblogs/demystifying-implicits-and-typeclasses-in-scala

http://danielwestheide.com/blog/2013/02/06/the-neophytes-guide-to-scala-part-12-type-classes.html

Scala allows an even more severe access restriction, with the private[this] qualifier:
private[this] var value = 0 // Accessing someObject.value is not allowed


5.4 Object-Private Fields

. Here, the ClassName must be the name of the class
being defined or an enclosing class. (

Martin Odersky suggests
to think about it this way: In Scala, classes take parameters, just like methods do


and the penny jar.


11/30

https://www.amazon.com/What-Now-Lieutenant-Leadership-Vietnam/dp/1937592642


PM -  things to do with quippysDataFrame.
'
QuerryS3T - the extract and transform gateway from Quippy10 works with the ZADScripts dependency.
. need the spark sql stuff in another dataframe.
. need the pivot table description
  Is group by n,rowcounter, pivot by dimensions  -  how to multiple group.
- need print garbage out of ZADScripts.

Some paragraphs 

-  the convergance of vertexs within same index.
-  the generator and the constant term...  
   2,4,8 / 10,100,1000
-  make zeros worksheet
   . choose index, degree_1, index, degree_2 both = n, subtract and zero appears
   . repeate with index'  then multiple together and two roots....

And ZADScripts.dfScripts needs the S* cypher ruberic.


the version of zadscripts on zeppelin is:

C:\Users\Aibes\JavaProjects\ZADScriptsK\ZADScripts\dist\ZADScripts.jar 

it need to return eTerm.

looks like case class s3t does have "rowCounter" field.

public ResultSet s3T(String rangeLow, String rangeHigh, String nMax, String dimension1, String dimension2, String dimension3, int tConstraint) 



  val d1 = "4"                                      
  val d2 = "4"                                      
  val d3 = "4"                                         //  Loop has hard range.  Todo:  Correctly parameterize.
  for( a <- 2 to 4;n <- 2 to 5;b <- 0 to (n-2)) {      //  a is dimension parameter as 2^a.  b is tConstraint parameter as 2^b
     terms +=rResult(n.toString,nMax,BigDecimal(2).pow(a).toString(),QuerryS3T.s3TQuerry(n.toString,n.toString,nMax:String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(b).toInt))
  }

  a iterates over dimension
  n iterates over range
  b iterates over tConstraint


public ResultSet s3T(String rangeLow, String rangeHigh, String nMax, String dimension1, String dimension2, String dimension3, int tConstraint) 

QuerryS3T.s3TQuerry(n.toString, n.toString, nMax:String, BigDecimal(2).pow(a).toString():String, BigDecimal(2).pow(a).toString():String, BigDecimal(2).pow(a).toString():String, BigDecimal(2).pow(b).toInt ) 

val tersSeq is the collection of s3T case classes.  
however, object QuerryS3T is only interested in result field.  It maps out the field then convertes it to string  

Once again:

object QuerryS3T is composed of a function s3TQuerry and a main method.

The main method loops a set product of parmameters for s3TQuerry.  
Each parameter set arguments a call to QuippyDB.  The resultSet 
arguments a case class sequence representing each resultSet row. 
the result field of cass class is collected and reduced then returned.  

A DataFrame is created from a case class sequence of each parameter set along with the
collected and reduced result.  However, it appears tConstraint is desired yet missing from DataFrame.  
The attempt is now to include the loop parameter representing tConstraint in df by first appending
the case class rResult and then including tConstraint parameter in each rResult.

 

case class rResult(
         n:String,
         maxN:String,
         dim:String,
         rowResult:BigDecimal
        ) 




rResult(
        n.toString,
        nMax,
        BigDecimal(2).pow(a).toString(),
        QuerryS3T.s3TQuerry(n.toString,n.toString,nMax:String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(a).toString():String,BigDecimal(2).pow(b).toInt)
        )



siamo fatti cosi

https://www.facebook.com/friends/?profile_id=100014902263689











































































https://getpocket.com/explore/item/what-happens-when-you-drink-a-gallon-of-water-a-day?utm_source=pocket-newtab
https://getpocket.com/explore/item/why-europeans-don-t-get-huge-medical-bills?utm_source=pocket-newtab




















 











file:///C:/Users/Aibes/ZeppelinDocs/Maven%20Dependency%20Scopes%20-%20HowToDoInJava.html


      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>


      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <relocations>
                <relocation>
                  <pattern>com.fasterxml.jackson</pattern>
                  <shadedPattern>aibes.com.fasterxml.jackson</shadedPattern>
                </relocation>
              </relocations>
            </configuration>
          </execution>
        </executions>
      </plugin>



      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
         </execution>
        </executions>
      </plugin>


All cryptographic protocols rest on assumptions—some, such as the famous RSA algorithm, depend on the widely held
 belief that standard computers will never be able to quickly factor the product of two large prime numbers.
 A cryptographic protocol is only as secure as its assumptions, and previous attempts at iO were built on untested and 
ultimately shaky foundations. 
The new protocol, by contrast, depends on security assumptions that have been widely used and studied in the past.
























































