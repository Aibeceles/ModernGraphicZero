%neo4j

MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='2' AND i.MaxN='6' AND i.Dimension="4"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)




WITH i,e,p,v,p1,p2,vv,iN,iM,p.Divisor as pD,CASE WHEN p2 IS null THEN 99 ELSE  toInteger(p2.Eval) END as p2Eval     //  the 99 default shoud be a negative for certainty.
//WITH {Divisor:pD,afe:toInteger(p2.Eval),tft:toInteger(p1.T),DS: COLLECT( {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
//                                      TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
//                                      ATwoSeq:toInteger(p2.T),
//                                      ADegree:toInteger(vv.Degree),   
//                                      AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })    }  AS argumentProduct,iN//,iM,e
Merge (cl:CollectedProductArgNode {Dimension:'4',MaxN:'6',N:'2',Divisor:pD,afe:p2Eval,tft:toInteger(p1.T)})                                                                          //N must agree with initial Match
CREATE (cl)-[:Arguments]->(aN:ArgumentsNode {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
                                                TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
                                                ATwoSeq:toInteger(p2.T),
                                                ADegree:toInteger(vv.Degree),   
                                                AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })

//Return pD, toInteger(p2.Eval) AS afe,p2Eval, toInteger(p1.T) AS tft,CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END
 as //TDegree,CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 //ELSE toFloat(v.Scalar) END as TScalar ,toInteger(p2.T) AS ATwoSeq,toInteger(vv.Degree)
 AS //ADegree,CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END as AScalar

ArgumentsNode.Divisor is pD aka p.divisor aka p:ProductArgsNode
CollectedProductArgNode.afe is p2Eval ak p2 aka ProductRelationA.Eval
CollectedProductArgNode.tft aka p1.T aka p1:ProductRelationT

//  iMaxN should = iiMaxN
//MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) 
//                     where i.N="${iN}" AND i.MaxN="${iMaxN}" AND i.Dimension="8" //AND t.twoSeq=1
MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) 
                     where i.N="5" AND i.MaxN="6" AND i.Dimension="8" //AND t.twoSeq=1
WITH i.N as iN, i.MaxN as iM,e,v,t                        
//OPTIONAL MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}),(tt:TwoSeqFactor)<-[]-(ii)
//                    where ii.MaxN="${iiM}"  AND ii.Dimension="2"
OPTIONAL MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}),(tt:TwoSeqFactor)<-[]-(ii)
                    where ii.MaxN="6"  AND ii.Dimension="8"                    
WITH iN,iM,e,v,vv,t,tt,ee
CREATE (p:ProductArgsNode {Divisor:t.twoSeq,Dimension:"8"})  
FOREACH(snoreMe IN CASE  WHEN vv.Vertex IS null  THEN [1] ELSE [] END | 
CREATE (v) <- [:ProductRelationT {T:t.twoSeq}] - (p) )
FOREACH(snoreMe IN CASE  WHEN vv.Vertex IS not null  THEN [1] ELSE [] END | 
CREATE (v) <- [:ProductRelationT {T:t.twoSeq}] - (p) 
CREATE (p) - [:ProductRelationA {T:tt.twoSeq, Eval:ee.Value}] -> (vv)  )
MERGE  (i:IndexedBy {N:iN, Dimension:"8",MaxN:iM,RowCounter:t.twoSeq})
MERGE  (i)-[:VertexIndexedBy]->(p)
MERGE (i) -[:IndexedByEvaluate]->(e)
return i,p,v



MERGE (i:IndexedBy {N:iN,RowCounter:eTrm,MaxN:iM,Dimension:"8"} )
MERGE (t:TwoSeqFactor {twoSeq:rpD2} )
MERGE (v:VertexNode {Scalar:rSLI.RScalar,Degree:rDRM} )
MERGE (e:Evaluate {Value:2^toInteger(iN)})                //   Must compute evaluate   or just say it is computing 2^n
MERGE  (i)-[ee:TwoFactor]->(t)
MERGE (i) -[:IndexedByEvaluate]->(e)
MERGE  (i)-[eee:VertexIndexedBy]->(v)


MATCH (v:VertexNode) <-[pR:ProductRelationT] -  (p:ProductArgsNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate) 
      where i.N="${iN}" AND i.MaxN="${iMaxN}" AND i.Dimension="4" 
WITH i.N as iN, i.MaxN as iM,e,p, pR.T as t                          
OPTIONAL MATCH (pp:ProductArgsNode)<-[]- (ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t)})
      where ii.MaxN="${iiMaxN}"  AND ii.Dimension="4"  // and ee.Value="4" 
WITH  iN,iM,e,p,pp,t,ee
CREATE (ppp:ProductArgsNode {Divisor:t,Dimension:"4"})                        //productArgsNode has divisor field.... is it used?? 
FOREACH(snoreMe IN CASE  WHEN pp IS null  THEN [1] ELSE [] END |              //The p.Divisor field looks like it is used....
CREATE (p) <- [:ProductRelationT {T:1}] - (ppp) )
FOREACH(snoreMe IN CASE  WHEN pp IS not null  THEN [1] ELSE [] END | 
CREATE (p) <- [:ProductRelationT {T:t}] - (ppp)    
CREATE (ppp) - [:ProductRelationA {T:t, Eval:ee.Value}] -> (pp)  )   Eval is a composite number.....
MERGE  (i:IndexedBy {N:iN, Dimension:"8",MaxN:iM,RowCounter:t})
MERGE  (i)-[:VertexIndexedBy]->(ppp)
MERGE (i) -[:IndexedByEvaluate]->(e)
return  count(ppp)

QuartetsRefactord 

Or maybe the thought of renaming rowCounter.....

MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='5' AND i.MaxN='6' AND i.Dimension="8"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)
Return i

MATCH (i:IndexedBy)-[]->(e:Evaluate)

MATCH (i:IndexedBy)-[]->(e:Evaluate) where i.N='5' AND i.MaxN='6' AND i.Dimension="8"
Return i

MATCH (i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='5' AND i.MaxN='6' AND i.Dimension="8"
return i

//   Will have to re-run from This preamble script 



Return of dimension 4      //  MERGE (t:TwoSeqFactor {twoSeq:rpD2} )  is wrong.  Cannot be divisor,  the first of three scripts dimension 8 will not propery 
                           // optional match.

MERGE (i:IndexedBy {N:iN,RowCounter:eTrm,MaxN:iM,Dimension:"8"} )
MERGE (t:TwoSeqFactor {twoSeq:rpD2} )                                //  Looks like t:TwoSeqFactor needs another field... the divisor.  
MERGE (v:VertexNode {Scalar:rSLI.RScalar,Degree:rDRM} )
MERGE (e:Evaluate {Value:2^toInteger(iN)})                //   Must compute evaluate   or just say it is computing 2^n
MERGE  (i)-[ee:TwoFactor]->(t)
MERGE (i) -[:IndexedByEvaluate]->(e)
MERGE  (i)-[eee:VertexIndexedBy]->(v)


First of three scripts dimension 8   

 //  This script is currently missunderstanding the match/optional match topology.  The TwoSequence node 


MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) 
                     where i.N="5" AND i.MaxN="6" AND i.Dimension="8"
WITH i.N as iN, i.MaxN as iM,e,v,t                        
OPTIONAL MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}),(tt:TwoSeqFactor)<-[]-(ii)
                    where ii.MaxN="6"  AND ii.Dimension="8"                    
WITH iN,iM,e,v,vv,t,tt,ee
CREATE (p:ProductArgsNode {Divisor:t.twoSeq,Dimension:"8"})  
FOREACH(snoreMe IN CASE  WHEN vv.Vertex IS null  THEN [1] ELSE [] END | 
CREATE (v) <- [:ProductRelationT {T:t.twoSeq}] - (p) )
FOREACH(snoreMe IN CASE  WHEN vv.Vertex IS not null  THEN [1] ELSE [] END | 
CREATE (v) <- [:ProductRelationT {T:t.twoSeq}] - (p) 
CREATE (p) - [:ProductRelationA {T:tt.twoSeq, Eval:ee.Value}] -> (vv)  )
MERGE  (i:IndexedBy {N:iN, Dimension:"8",MaxN:iM,RowCounter:t.twoSeq})
MERGE  (i)-[:VertexIndexedBy]->(p)
MERGE (i) -[:IndexedByEvaluate]->(e)
return i,p,v

Second of three scipts dimension 8

MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='5' AND i.MaxN='6' AND i.Dimension="8"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)
WITH i,e,p,v,p1,p2,vv,iN,iM,p.Divisor as pD,CASE WHEN p2 IS null THEN 99 ELSE  toInteger(p2.Eval) END as p2Eval     //  the 99 default shoud be a negative for certainty.

Merge (cl:CollectedProductArgNode {Dimension:'8',MaxN:'6',N:'5',Divisor:pD,afe:p2Eval,tft:toInteger(p1.T)})                                                                          //N must agree with initial Match
CREATE (cl)-[:Arguments]->(aN:ArgumentsNode {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
                                                TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
                                                ATwoSeq:toInteger(p2.T),
                                                ADegree:toInteger(vv.Degree),   
                                                AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })

//Return pD, toInteger(p2.Eval) AS afe,p2Eval, toInteger(p1.T) AS tft,CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 
 ELSE toInteger(v.Degree) END as //TDegree,CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 //ELSE toFloat(v.Scalar) END
 as TScalar ,toInteger(p2.T) AS ATwoSeq,toInteger(vv.Degree) AS //ADegree,CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 
 ELSE toFloat(vv.Scalar) END as AScalar



Third of three scripts dimension 8


MATCH (cl:CollectedProductArgNode {Dimension:'8',MaxN:'6',N:'5'})-[:Arguments]->(aN:ArgumentsNode)
WITH {Divisor:cl.Divisor,afe:cl.afe,tft:cl.tft,DS: COLLECT( {  TDegree:aN.TDegree,
                                      TScalar:aN.TScalar,
                                      ATwoSeq:aN.ATwoSeq,
                                      ADegree:aN.ADegree,   
                                      AScalar:aN.AScalar})}  AS argumentProduct,cl.N as iN,cl.MaxN as iM
WITH COLLECT(argumentProduct) as argumentProductt,argumentProduct.tft as aPafe,iN,iM,argumentProduct.Divisor as apD,argumentProduct.afe AS aPclafe
UNWIND RANGE(0,SIZE(argumentProductt)-1) as iii
WITH argumentProductt[iii].DS as aPTds,aPafe,iN,iM,apD,aPclafe
UNWIND aPTds as aPTdsU
WITH CASE WHEN aPclafe = 99 THEN                                   //  important when collecting different dimensions.                                                          
{DS:COLLECT({ResultScalar:(toFloat(aPTdsU.TScalar)*toInteger(aPafe)), 
      ResultDegree:aPTdsU.TDegree,
      ResultTwoSeq:aPTdsU.tft}),ET:aPafe,Divisor:apD}
ELSE
{DS:COLLECT({ResultScalar:(toFloat(aPTdsU.TScalar)*toFloat(aPTdsU.AScalar)*aPTdsU.ATwoSeq), 
      ResultDegree: CASE WHEN aPTdsU.ADegree=-1 THEN aPTdsU.TDegree ELSE aPTdsU.ADegree+aPTdsU.TDegree END,
      ResultTwoSeq:aPTdsU.ATwoSeq}),ET:aPafe,Divisor:apD}
END
AS resultProduct,iN,iM
WITH COLLECT(resultProduct) as rPC,resultProduct.ET as eT,iN,iM,resultProduct.Divisor as rpD1
UNWIND RANGE(0,SIZE(rPC)-1) as iI
WITH rPC[iI].DS as rPCDS,eT,iN,iM,rpD1
UNWIND rPCDS as rPCDSU
WITH {RScalarList:COLLECT({RScalar:rPCDSU.ResultScalar}),RDegree:rPCDSU.ResultDegree,ET:eT,Divisor:rpD1} as resultMap,iN,iM

WITH COLLECT(resultMap) as resultMapC,resultMap.ET as eTrm,resultMap.RDegree as rDRM,iN,iM,resultMap.Divisor as rpD2
UNWIND RANGE(0,SIZE(resultMapC)-1) as iII
WITH resultMapC[iII].RScalarList as rSL,eTrm,rDRM,iN,iM,rpD2
UNWIND RANGE(0,SIZE(rSL)-1) as iIII
UNWIND rSL[iIII] as rSLI
WITH COLLECT(rSLI.RScalar) as cts,eTrm,rDRM,iN,iM,rpD2
WITH REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,eTrm,rDRM,iN,iM,rpD2
With {Degree:rDRM,Divisor:rpD2,Scalar:Collect(TotalRScalar)} AS pTerms
WITH pTerms.Scalar as pTermsS,pTerms.Degree as pTermsD,pTerms.Divisor as pTermsDiv
With REDUCE(tSum=0,ff IN pTermsS | tSum +ff) As TRScalar,pTermsD,pTermsDiv
RETURN TRScalar,pTermsD,pTermsDiv


12/13
PM

Delete quartets
Redraw the quartets 

Try the octets.

Firstly:
The Narrative.


MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) 
                     where i.N="5" AND i.MaxN="6" AND i.Dimension="8" //AND t.twoSeq=1
WITH i.N as iN, i.MaxN as iM,e,v,t,i                       

MATCH (ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}) where ii.MaxN="6"  AND ii.Dimension="8"  
//OPTIONAL MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}),(tt:TwoSeqFactor)<-[]-(ii)
//                    where ii.MaxN="6"  AND ii.Dimension="8"                    
RETURN ii,ee


(no changes, no records)


MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) 
                     where i.N="5" AND i.MaxN="6" AND i.Dimension="8" //AND t.twoSeq=1
WITH i.N as iN, i.MaxN as iM,e,v,t,i                       
//OPTIONAL MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}),(tt:TwoSeqFactor)<-[]-(ii)
//                    where ii.MaxN="${iiM}"  AND ii.Dimension="2"
MATCH (ee:Evaluate {Value:toString(t.twoSeq)})
//OPTIONAL MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:toString(t.twoSeq)}),(tt:TwoSeqFactor)<-[]-(ii)
//                    where ii.MaxN="6"  AND ii.Dimension="8"                    
RETURN ee


│{"Value":"4"}│
├─────────────┤
│{"Value":"8"}│
├─────────────┤


MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='5' AND i.MaxN='6' AND i.Dimension="4"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)
WITH i,e,p,v,p1,p2,vv,iN,iM,p.Divisor as pD,CASE WHEN p2 IS null THEN 99 ELSE  toInteger(p2.Eval) END as p2Eval     //  the 99 default shoud be a negative for certainty.
//WITH {Divisor:pD,afe:toInteger(p2.Eval),tft:toInteger(p1.T),DS: COLLECT( {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
//                                      TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
//                                      ATwoSeq:toInteger(p2.T),
//                                      ADegree:toInteger(vv.Degree),   
//                                      AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })    }  AS argumentProduct,iN//,iM,e
Merge (cl:CollectedProductArgNode {Dimension:'4',MaxN:'6',N:'5',Divisor:pD,afe:p2Eval,tft:toInteger(p1.T)})                                                                          //N must agree with initial Match
CREATE (cl)-[:Arguments]->(aN:ArgumentsNode {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
                                                TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
                                                ATwoSeq:toInteger(p2.T),
                                                ADegree:toInteger(vv.Degree),   
                                                AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })


Added 105 labels, created 105 nodes, set 490 properties, created 101 relationships, completed after 134 ms.



MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='4' AND i.MaxN='6' AND i.Dimension="4"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)
WITH i,e,p,v,p1,p2,vv,iN,iM,p.Divisor as pD,CASE WHEN p2 IS null THEN 99 ELSE  toInteger(p2.Eval) END as p2Eval     //  the 99 default shoud be a negative for certainty.
//WITH {Divisor:pD,afe:toInteger(p2.Eval),tft:toInteger(p1.T),DS: COLLECT( {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
//                                      TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
//                                      ATwoSeq:toInteger(p2.T),
//                                      ADegree:toInteger(vv.Degree),   
//                                      AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })    }  AS argumentProduct,iN//,iM,e
Merge (cl:CollectedProductArgNode {Dimension:'4',MaxN:'6',N:'4',Divisor:pD,afe:p2Eval,tft:toInteger(p1.T)})                                                                          //N must agree with initial Match
CREATE (cl)-[:Arguments]->(aN:ArgumentsNode {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
                                                TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
                                                ATwoSeq:toInteger(p2.T),
                                                ADegree:toInteger(vv.Degree),   
                                                AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })


Added 52 labels, created 52 nodes, set 224 properties, created 49 relationships, completed after 95 ms.



MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='3' AND i.MaxN='6' AND i.Dimension="4"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)
WITH i,e,p,v,p1,p2,vv,iN,iM,p.Divisor as pD,CASE WHEN p2 IS null THEN 99 ELSE  toInteger(p2.Eval) END as p2Eval     //  the 99 default shoud be a negative for certainty.
//WITH {Divisor:pD,afe:toInteger(p2.Eval),tft:toInteger(p1.T),DS: COLLECT( {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
//                                      TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
//                                      ATwoSeq:toInteger(p2.T),
//                                      ADegree:toInteger(vv.Degree),   
//                                      AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })    }  AS argumentProduct,iN//,iM,e
Merge (cl:CollectedProductArgNode {Dimension:'4',MaxN:'6',N:'3',Divisor:pD,afe:p2Eval,tft:toInteger(p1.T)})                                                                          //N must agree with initial Match
CREATE (cl)-[:Arguments]->(aN:ArgumentsNode {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
                                                TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
                                                ATwoSeq:toInteger(p2.T),
                                                ADegree:toInteger(vv.Degree),   
                                                AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })


Added 15 labels, created 15 nodes, set 38 properties, created 13 relationships, completed after 21 ms.

MATCH (i:IndexedBy)-[]->(e:Evaluate),(i:IndexedBy)-[:VertexIndexedBy]->(p:ProductArgsNode) where i.N='2' AND i.MaxN='6' AND i.Dimension="4"                                      //  Must aggree with Merge clause.
WITH i,e,p,i.N as iN, i.MaxN as iM
MATCH (v:VertexNode) <- [p1:ProductRelationT] - (p) 
WITH i,e,p,v,p1,iN,iM
OPTIONAL MATCH  (p) - [p2:ProductRelationA] -> (vv:VertexNode)
WITH i,e,p,v,p1,p2,vv,iN,iM,p.Divisor as pD,CASE WHEN p2 IS null THEN 99 ELSE  toInteger(p2.Eval) END as p2Eval     //  the 99 default shoud be a negative for certainty.
//WITH {Divisor:pD,afe:toInteger(p2.Eval),tft:toInteger(p1.T),DS: COLLECT( {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
//                                      TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
//                                      ATwoSeq:toInteger(p2.T),
//                                      ADegree:toInteger(vv.Degree),   
//                                      AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })    }  AS argumentProduct,iN//,iM,e
Merge (cl:CollectedProductArgNode {Dimension:'4',MaxN:'6',N:'2',Divisor:pD,afe:p2Eval,tft:toInteger(p1.T)})                                                                          //N must agree with initial Match
CREATE (cl)-[:Arguments]->(aN:ArgumentsNode {  TDegree:CASE WHEN toInteger(v.Degree)=-1 THEN toInteger(v.Degree)+1 ELSE toInteger(v.Degree) END,
                                                TScalar:CASE WHEN toInteger(v.Degree)=-1 THEN toFloat(v.Scalar)*2 ELSE toFloat(v.Scalar) END,
                                                ATwoSeq:toInteger(p2.T),
                                                ADegree:toInteger(vv.Degree),   
                                                AScalar:CASE WHEN toInteger(vv.Degree)=-1 THEN toFloat(vv.Scalar)*2 ELSE toFloat(vv.Scalar) END })


Added 10 labels, created 10 nodes, set 24 properties, created 9 relationships, completed after 12 ms.


Third script dimension 4 cannot merge vertex nodes,  It must create node for each rSLI 
in rSL.


mEANING after lunch read read again....


match (p:ProductArgsNode {Dimension:'2'})
Detach delete(p)
Deleted 357 nodes, deleted 5499 relationships, completed after 170 ms.
















