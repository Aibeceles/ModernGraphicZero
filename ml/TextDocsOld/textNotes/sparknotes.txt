the google file system.


page 7/31

spark.read ...

= spark.read.csv("s3://...")


data science and
data engineering tasks


data scientists
have to cleanse the data, explore it to discover patterns, and build models to predict
14 | Chapter 1: Introduction to Apache Spark: A Unied Analytics Engine
or suggest outcomes. S

RDDs,
DataFrames, or Datasets—that suit the task at hand, 


In Spark 2.0, the SparkSession became a unified conduit to all Spark operations and
data

You express what you want the task or operation to compute, not how to com‐
pute it

 build complex data pipelines that enable extract,
transform, and load (ETL) data

tarball

Spark’s Directories and Files

scala> val strings = spark.read.text("../README.md")
strings: org.apache.spark.sql.DataFrame = [value: string]

failed to initialize complier: object java.land.object in compiler mirror not found.
note that oas of 2.8 scala does not assume use of the java classpath
for the old behaviour pas -usejavacp to scala, or if using a settings object programmatically, settings.usejavacp.value = true.

Transformations, Actions, and Lazy Evaluation | 29

JSON, CSV, Parquet, Text, Avro, ORC

so difference between Parquet and JSON is... Parquet supports compression, schema available...

dataframreader read .filetype
dataframwriter write .filetype

p87


c02_level: big

serialization, and deserialization of the JVM objects

If you want to take advantage of Catalyst optimization and benefit from Tung‐
sten’s efficient code generation and serialization with encoders, use Datasets.

 a DDL-formatted
string

DataFrameReader
DataFrameReader is the core construct for reading data from a data source into a
DataFrame. It has a defined format and a recommended pattern for usage:

Unlike with DataFrameReader, you access its instance not
from a SparkSession but from the DataFrame you wish to save. It has a few recom‐
mended usage patterns:


none, uncompressed,
bzip2, deflate, gzip,
lz4, or snappy


Common DataFrames and Spark SQL Operations
Part of the power of Spark SQL comes from the wide range of DataFrame operations
(also known as untyped Dataset operations) it supports. The list of operations is quite
extensive and includes:
• Aggregate functions
• Collection functions
• Datetime functions
• Math functions
• Miscellaneous functions
• Non-aggregate functions
• Sorting functions
• String functions
• UDF functions
• Window functions

Pivoting
When working with your data, sometimes you will need to swap the columns for the
rows

We explored Spark’s built-in functions for complex data types


Encoder<Usage> usageEncoder = Encoders.bean(Usage.class);
import org.apache.spark.sql.Encoders;

import org.apache.commons.lang3.RandomStringUtils
vs yea old rndint.

dsUsage.map((MapFunction<Usage, Double>) u

These configurations affect three Spark components: the
Spark driver, the executor, and the shuffle service running on the executor

OOM errors

// Create DataFrames
val usersDF = (0 to 1000000).map(id => (id, s"user_${id}",

 join scheme 

demonstrated how by bucketing Data‐
Frames by sorted keys you can skip over expensive shuffle operations.

Structured Streaming vs kafka
Complete details on the output modes supported by different quer‐
ies can be found in the latest Structured Streaming Programming
Guide.
Estimators to Build Models
are eagerly evaluated (i.e., kick off Spark jobs)


Features with Transformers

BigInt.probablePrime(100, scala.util.Random)

mutable/immutable functions.

curring

locks and threads - the criticisim.


combinators

tail recursive functions


xs match {   }


not type refinement - statements
says the compiler.

don't use procedure syntax.

the expression problem
pattern matchinng vs dynamic dispatch.

implicit decorators


implicit conversions are problematic

type class and static something..


scala monads
Option type

for  yield expressions

require

config:Config


composing monads

algebraic effects in functional programming.

curry vs closure.


implicit function types

Strategic Scala Style
principle of least power

about strategic scala style.

monoid create complexity thing f o g
set of thing plus a rule that obeys other rules for combining them.

functions of type a return a transform of a M a (calling a constructor for m with a)

functions live in monoids
data lives in monads.

https://www.youtube.com/watch?v=ZhuHCtR3xq8

guy steel growing a language.



hope you found some fun completing your trip around the sun.

If you're a Java programmer and think you'll never see this in the real world then you might
 be surprised. If you're using the nice Optional type that comes with Java 8 then you're using
 monads. The Optional.of() function is equivalent to 'return' in the Maybe example and
 Optional.flatMap() is equivalent to the '>>=' sequencing operator. Understanding that made
 the whole concept of monads finally 'click' for me.

tail recursion again


function values in programming languages.


sbt

sbt consol is repl


the substitution model cannot be applied
if the function has a side effect.

to courses










reddit 
user name dievos
password ChiefQuippy531




























































