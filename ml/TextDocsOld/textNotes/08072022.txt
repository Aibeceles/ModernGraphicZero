Docker stuff...

Set up the docker run time on the server (a one time effort).


docker ps       - see all running containers  


docker image
the actual package
the artifact that can be moved around.

docker container
start the image on a machine... a container.


docker run <program name>


docker virtualizes the application layer.  not os. not hardware.

docker toolbox abstracts kernal enabeling docker to run different docker images.

docker natively runs on windows 10.  -all good.

the docker engine important to run the docker containers.

docker compose when the envorionment has multiple containers.

on windows, virtualizaton must be enabled.
hypervisor must be installed.





Advanced package tool

update the apt package index

sudo apt-get update

install packages to allow apt to use a repository of https

apt-get install \ 
  apt-transport-https \
  ca-certificates \
  curl \
  software-properties-common

add docker's official GPG key



container is a running environment for image.

image needs a file system..
for logging
for store some configuration files..
for environmental configuration like envorinmental variables.


container has port 5000 
which makes it possible to talk to the application.



but if you have a dependency on a specific version of docker


how many containers at once can docker manage...

in the last video created and started two containgers mogodb and mongo db express....



docker run command 

docker run -d\
-name
-p             ports
-e            user name
-e            password
--net mongo-network

but lets say several docker containers need to talk to each other.  
start them up ect... 
then use docker compose....

mongo-docker-compose.yaml

mersion:'3'  
services:
   mongodb:         mapps to container name
      image:        which image the container will be built from.
      ports:        docker port ":" application port
       = 27017:27017
      environment:
       - Mongo.._Username=admin

so have docker-compose.yaml and how to start containters with it.

use docker-compose          command is already installed with docker.

as example:
docker-compose -f mongo.yaml up
                             down



the blueprint for building images  --   a docker file.

docker file 

From node    h- can ron a node application.

ENV mongo_db_username=admin  \
    mongo_db_pwd=passworord

RUN mkdir -p/home/app               -- directory is created INSIDE container.
                                    -- note run command allows any 
                                       linux command.  In this case, mkdir


COPY ./home/app   source target    note copy executs on host machine.

CMD ["node","server.js"]     



note docker rmi <image #>

docker ps -a | grep my-app

docer ps

docker exec -t <docker id> /bin/sh   maybe  docker exe -it <dockdr id>  /bin/bash

from the docker terminal...  the above opened the docker terminal
can 
env    - will list environment variables...


amazon ecr 

if the docker image was built from a jenkins server...

docker volumes

the container file directory is replicated on host file directory.


three types:

named volumes should be used in production...


and then my friend, container orchestraton tools.... kubernetics....














basic docker commands

docker pull
docker run
docker start
docker stop
docker ps

more docker commands
docker images


docker run redis  - for example redis - so applicaton can 
                    connect to it.


cntrl-c stops the application


docker run -d redis run in detached mode...  that is reuse terminal.

docker ps -a      images running or not running...


how to use any container just started???


in the logs of the container....


docker run -d p6001:6379 --name redis-older redis.4.1

logs redis-older


docker exec -it <container_id> /bin/bash/           -it interactive terminal 

//the curser will change
will be inside the container as root user.

pwd    print which directory.

cd /     change to home directory
ls 

env      print all the environment variables.

exit    //  exits the terminal


since lightweight  maybe no curl function...


difference between 

docker run create a new container from an image..

and

docker start working with containers.
             
            -d , and , -p the port binding

docker ps -a     all images....



docker network - where the multiple containers are running.
when two containers are deployed in the same network..

they can communicate by name no localhost port...

node js, for instance will communicate via localhost:...


docker network ls


docker network create mongo-network   // for instance....

docker run -p 27017:27017 -d mongo
              host  container   can be same... and run in detach mode...















...







  























