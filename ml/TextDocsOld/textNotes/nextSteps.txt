4/25

   What does further dimensional substitution look like???  --  Not for today. 

   The current ZADDB only includes i.MaxN=8.  Want MaxN=9,10,11,7,6,5,4,3,2...
   And by the way, TwoPolynomial033119.ods says cypher for twoSeq substitution is correctly working (up to diligence).

   And today's lab seems to be ahh a lab day.  
   More like get a nice .ods data set for some old fashion pivot table analysis.

 ZAD is to be wrapped as a jar and run from command line with args[] defining behavior.

 plvManager mage = new plvManager(tlSemaphore,t2Semaphore,cosetSemaphore,dbVector,dbVector1,dbVectorCST,tl1Semaphore,dbVector11);
 zaddbTable1 bmage = new zaddbTable1(tlSemaphore,dbVector,mage);

 public class plvManager extends Thread{
 BigDecimal NN = new BigDecimal(8);
 plvRunThread mage = new plvRunThread(2,8,NN,tlSem,t2Sem,cosetSem,dbbVector,dbb1Vector,dbbCSTVector,tl1Sem,dbbVector11);

 Note nn, mage constructor argument list.  (a current observation not point of action).
 Also, plvManager has hard coded but commented out concurrency.  Is this the final state of a wrapped ZAD?

 LoopsDriverTwoPManager lDriver= new LoopsDriverTwoPManager(muList,muSem,loopsLogic,tl1Semaphore,dbVector11,mage,tl1Semaphore,dbVector11,true);  
 GaussTable1 gTable1 = new GaussTable1(muSem,muList,lDriver);

public class GaussTable1 extends Thread {
    private Semaphore tlSem;  
    public static final String PROP_SAMPLE_PROPERTY = "sampleProperty";
    private String sampleProperty;
    private PropertyChangeSupport propertySupport;
    private String framework = "embedded";

    
    private String driver = "org.neo4j:neo4j-jdbc-driver:3.3.0";
    private String protocol = "jdbc:neo4j:bolt://localhost";

    String csvFile = "zad032819.csv";


Notice:

MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="5" 

The lead pattern match in latest dimensional cypher assumes a particular nMax.
Must include nMax constraint.  
Would want an all in cypher but....

So, intend to one at a time and hand work the .ods

Have nMax=8-14 on ZADDB
for each nMax need from 4 up to Nmax-1

so, start with 8
no 1009???
   1109

Call apoc.export.csv.query("MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) RETURN v.Vertex,v.Scalar,v.Degree,i,RowCounter,i.N,i.MaxN,t.twoSeq ",
"TwoPolynomial032719.csv",{d:";"})


Call apoc.export.csv.query("
MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N='8' AND i.MaxN='14'
WITH {DS:collect({Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)/2}),T:toString(t.twoSeq)} as targetFactors,i.N as iN, i.MaxN as iM  
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:targetFactors.T}),(tt:TwoSeqFactor)<-[]-(ii) where ii.MaxN='14'
WITH {DS:Collect({TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:CASE WHEN vv.Degree = '-1' THEN toFloat(vv.Scalar) ELSE toFloat(vv.Scalar)/2 END,RowC:ii.RowCounter}),Evaluate:ee.Value}  as argumentFactors  ,targetFactors,iN,iM
WITH COLLECT(targetFactors) as targetF,targetFactors.T as tFT,argumentFactors,iN,iM 
WITH COLLECT(argumentFactors) as argumentF,argumentFactors.Evaluate as aFE,targetF,tFT,iN,iM
UNWIND RANGE(0,SIZE(targetF)-1) as ii
WITH targetF[ii].DS as tFDS, argumentF[ii].DS as aFDS,tFT,aFE,iN,iM

UNWIND tFDS as tFDSU
UNWIND aFDS as aFDSU
WITH {afe:aFE,tft:tFT,DS: COLLECT( {  TDegree:tFDSU.Degree,TScalar:tFDSU.Scalar,ATwoSeq:aFDSU.TT,ADegree: CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Degree+1 ELSE aFDSU.Degree END,AScalar:aFDSU.Scalar })    }  AS argumentProduct,iN,iM

WITH COLLECT(argumentProduct) as argumentProductt,argumentProduct.afe as aPafe,iN,iM
UNWIND RANGE(0,SIZE(argumentProductt)-1) as iii

WITH argumentProductt[iii].DS as aPTds,aPafe,iN,iM
UNWIND aPTds as aPTdsU
WITH {DS:COLLECT({  ResultScalar:aPTdsU.TScalar*aPTdsU.AScalar*aPTdsU.ATwoSeq,  ResultDegree:aPTdsU.ADegree+aPTdsU.TDegree,  ResultTwoSeq:aPTdsU.ATwoSeq}),ET:aPafe} as resultProduct,iN,iM
WITH COLLECT(resultProduct) as rPC,resultProduct.ET as eT,iN,iM
UNWIND RANGE(0,SIZE(rPC)-1) as iI

WITH rPC[iI].DS as rPCDS,eT,iN,iM
UNWIND rPCDS as rPCDSU
WITH {RScalarList:COLLECT({RScalar:rPCDSU.ResultScalar}),RDegree:rPCDSU.ResultDegree,ET:eT} as resultMap,iN,iM
WITH COLLECT(resultMap) as resultMapC,resultMap.ET as eTrm,resultMap.RDegree as rDRM,iN,iM

UNWIND RANGE(0,SIZE(resultMapC)-1) as iII
WITH resultMapC[iII].RScalarList as rSL,eTrm,rDRM,iN,iM
UNWIND RANGE(0,SIZE(rSL)-1) as iIII
UNWIND rSL[iIII] as rSLI
WITH COLLECT(rSLI.RScalar) as cts,eTrm,rDRM,iN,iM
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,eTrm,rDRM,iN,iM
RETURN TotalRScalar,eTrm,rDRM,iN,iM
 ",
"quartets1408.csv",{d:";"})


TwoPolynomial033119:   The scalars,twoSeq's composing a twoPolynomial.
TwoPolynomial042619:   The result of TP033119 twoSeq substitution. A data error is revealed.   
                       It does not reveal cypher.argumentProduct, needed for data error fix

Need argumentProduct.cvs (As below):










Call apoc.export.csv.query("
MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N='7' AND i.MaxN='8'
WITH {DS:collect({Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)/2}),T:toString(t.twoSeq)} as targetFactors,i.N as iN, i.MaxN as iM  
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:targetFactors.T}),(tt:TwoSeqFactor)<-[]-(ii) where ii.MaxN='14'
WITH {DS:Collect({TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:CASE WHEN vv.Degree = '-1' THEN toFloat(vv.Scalar) ELSE toFloat(vv.Scalar)/2 END,RowC:ii.RowCounter}),Evaluate:ee.Value}  as argumentFactors  ,targetFactors,iN,iM
WITH COLLECT(targetFactors) as targetF,targetFactors.T as tFT,argumentFactors,iN,iM 
WITH COLLECT(argumentFactors) as argumentF,argumentFactors.Evaluate as aFE,targetF,tFT,iN,iM
UNWIND RANGE(0,SIZE(targetF)-1) as ii
WITH targetF[ii].DS as tFDS, argumentF[ii].DS as aFDS,tFT,aFE,iN,iM
UNWIND tFDS as tFDSU
UNWIND aFDS as aFDSU
return aFE,tFT,tFDSU.Degree,tFDSU.Scalar,aFDSU.TT,CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Degree+1 ELSE aFDSU.Degree END as aFDSU.Degree,aFDSU.Scalar,iN,iM
 ",
"argumentProduct0807.csv",{d:";"})


WITH {afe:aFE,tft:tFT,DS: COLLECT( {  TDegree:tFDSU.Degree,TScalar:tFDSU.Scalar,ATwoSeq:aFDSU.TT,ADegree: CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Degree+1 ELSE aFDSU.Degree END,AScalar:aFDSU.Scalar })    }  AS aP,iN,iM
   
      

RETURN TotalRScalar,eTrm,rDRM,iN,iM
 ",
"quartets1408.csv",{d:";"})


4/27

argumentProduct0807.cvs has target (scalar/degree).  Need to see the corrosponding argument (scalar/degree).
So, maybe an ugly one d pivot table.

4/28

MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N='7' AND i.MaxN='8'
WITH {DS:collect({Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)/2}),T:toString(t.twoSeq)} as targetFactors,i.N as iN, i.MaxN as iM  
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:targetFactors.T}),(tt:TwoSeqFactor)<-[]-(ii) where ii.MaxN='14'
WITH {DS:Collect({TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:CASE WHEN vv.Degree = '-1' THEN toFloat(vv.Scalar) ELSE toFloat(vv.Scalar)/2 END,RowC:ii.RowCounter}),Evaluate:ee.Value}  as argumentFactors  ,targetFactors,iN,iM
WITH COLLECT(targetFactors) as targetF,targetFactors.T as tFT,argumentFactors,iN,iM 
WITH COLLECT(argumentFactors) as argumentF,argumentFactors.Evaluate as aFE,targetF,tFT,iN,iM
UNWIND RANGE(0,SIZE(targetF)-1) as ii
WITH targetF[ii].DS as tFDS, argumentF[ii].DS as aFDS,tFT,aFE,iN,iM
UNWIND tFDS as tFDSU
UNWIND aFDS as aFDSU
return aFE,tFT,tFDSU.Degree,tFDSU.Scalar,aFDSU.TT,CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Degree+1 ELSE aFDSU.Degree END as aFDSUDegree,aFDSU.Scalar,iN,iM


4/29

  Begin the audit.

TwoPolynomial042619 has:
  PivotTable_quartets_1.  (i.M,i.N,Evaluate) is crossed with (Degree) and Result is tabulated.
  PivotTable_graphdb_1.   (i.MaxN,i.N,t.twoSeq) is crossed with (Degree) and Result is tabulated.

Whenever   (i.M,i.N,Evaluate) =  (i.MaxN,i.N,t.twoSeq) Then Result_quartets = Result_Degree.

But an error is propagated...

Within the Algorithem:

   Have, as per graphdb, for each twoSeq in (i.MaxN,i.N) a set of vertices with degree <3 as multiplication arguments. 
   Each twoSeq can be substituted with it's representative graphdb vertex set for the multiplication arguments.

Need to prove out the multiplication table.

   There is in (iMaxN,i.N) one vertex for each degree.  It is the argument for each vertex in the twoSeq substitution.

So....

  (i.M,i.N,twoSeq) = (8,7,16). There are four vertex's(scalar,degree) each to be multiplied through by the set of verticies
  representing twoSeq=16.

  

MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="8" AND t.twoSeq=16 AND i.MaxN='9'
WITH [{T:t.twoSeq,Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}] as targetFactors
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate),(tt:TwoSeqFactor)<-[]-(ii) where ii.N="5" AND ii.MaxN='9'
WITH [{TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar)}]
      as argumentFactors,targetFactors
UNWIND argumentFactors as argumentF
UNWIND targetFactors as targetF
WITH [{TDegree:targetF.Degree,TScalar:targetF.Scalar,ATwoSeq:argumentF.TT,ADegree:argumentF.Degree,AScalar:argumentF.Scalar}]  AS argumentProduct
UNWIND argumentProduct as argumentProductt
WITH [{ResultScalar:CASE WHEN argumentProductt.ADegree=-1 THEN (argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/2 ELSE 
        (argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/4 END,
      ResultDegree: CASE WHEN argumentProductt.ADegree=-1 THEN argumentProductt.TDegree ELSE argumentProductt.ADegree+argumentProductt.TDegree END,
      ResultTwoSeq:argumentProductt.ATwoSeq}] as resultProduct
UNWIND resultProduct as rP
return rP.ResultScalar,rP.ResultDegree

//WITH {RScalarList:COLLECT({RScalar:rP.ResultScalar}),RDegree:rP.ResultDegree} as ResultMap
//WITH COLLECT(ResultMap.RScalarList) AS RScalar,ResultMap
//Unwind RANGE(0,SIZE(RScalar)-1) as ii
//Unwind RScalar[ii] as ts
//With collect(ts.RScalar) as cts,ResultMap
//With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,ResultMap
//With [ResultMap.RDegree,TotalRScalar] as output
//Return output

rP.ResultScalar	rP.ResultDegree
24.5	2
-147.0	1
-3.5	3
21.0	2
-21.0	1
8.75	2
-35.0	1
-1.75	3
7.0	2
10.5	2
-52.5	1
-1.75	3
8.75	2
56.0	2
-392.0	1
-7.0	3
49.0	2
126.0	2
-1008.0	1
-14.0	3
112.0	2
-147.0	1
882.0	0
21.0	2
-126.0	1
126.0	0
-52.5	1
210.0	0
10.5	2
-42.0	1
-63.0	1
315.0	0
10.5	2
-52.5	1
-336.0	1
2352.0	0
42.0	2
-294.0	1
-756.0	1
6048.0	0
84.0	2
-672.0	1
-3.5	3
21.0	2
0.5	4
-3.0	3
3.0	2
-1.25	3
5.0	2
0.25	4
-1.0	3
-1.5	3
7.5	2
0.25	4
-1.25	3
-8.0	3
56.0	2
1.0	4
-7.0	3
-18.0	3
144.0	2
2.0	4
-16.0	3
21.0	2
-126.0	1
-3.0	3
18.0	2
-18.0	1
7.5	2
-30.0	1
-1.5	3
6.0	2
9.0	2
-45.0	1
-1.5	3
7.5	2
48.0	2
-336.0	1
-6.0	3
42.0	2
108.0	2
-864.0	1
-12.0	3
96.0	2


MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="8" AND t.twoSeq=16 AND i.MaxN='9'
WITH [{T:t.twoSeq,Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}] as targetFactors
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate),(tt:TwoSeqFactor)<-[]-(ii) where ii.N="5" AND ii.MaxN='9'
WITH [{TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar)}]
      as argumentFactors,targetFactors
UNWIND argumentFactors as argumentF
UNWIND targetFactors as targetF
WITH [{TDegree:targetF.Degree,TScalar:targetF.Scalar,ATwoSeq:argumentF.TT,ADegree:argumentF.Degree,AScalar:argumentF.Scalar}]  AS argumentProduct
UNWIND argumentProduct as argumentProductt
RETURN argumentProductt.TDegree,  argumentProductt.TScalar,   argumentProductt.ATwoSeq,   argumentProductt.ADegree,    argumentProductt.AScalar



UNWIND argumentProduct as argumentProductt
WITH [{ResultScalar:CASE WHEN argumentProductt.ADegree=-1 THEN (argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/2 ELSE 
        (argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/4 END,
      ResultDegree: CASE WHEN argumentProductt.ADegree=-1 THEN argumentProductt.TDegree ELSE argumentProductt.ADegree+argumentProductt.TDegree END,
      ResultTwoSeq:argumentProductt.ATwoSeq}] as resultProduct
UNWIND resultProduct as rP
return rP.ResultScalar,rP.ResultDegree

//WITH {RScalarList:COLLECT({RScalar:rP.ResultScalar}),RDegree:rP.ResultDegree} as ResultMap
//WITH COLLECT(ResultMap.RScalarList) AS RScalar,ResultMap
//Unwind RANGE(0,SIZE(RScalar)-1) as ii
//Unwind RScalar[ii] as ts
//With collect(ts.RScalar) as cts,ResultMap
//With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,ResultMap
//With [ResultMap.RDegree,TotalRScalar] as output
//Return output


MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="8" AND t.twoSeq=32 AND i.MaxN='9'

WITH [{T:t.twoSeq,Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar),TargetResult:((i.MaxN^v.Degree)*v.Scalar)/2},MaxN:toInteger(i.MaxN)] as targetFactors

MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate),(tt:TwoSeqFactor)<-[]-(ii) where ii.N="5" AND ii.MaxN='9'

WITH [{TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar)}]
      as argumentFactors,targetFactors

UNWIND argumentFactors as argumentF
UNWIND targetFactors as targetF

WITH [{TargetResult:CASE WHEN argumentF.Degree=-1 THEN (targetF.MaxN*argumentF.TT*argumentF.Scalar)*targetF.TargetResult ELSE (((targetF.MaxN^argumentF.Degree)*argumentF.TT*argumentF.Scalar)/2)*targetF.TargetResult END}]  as ResultProduct    


WITH [{TDegree:targetF.Degree,TScalar:targetF.Scalar,ATwoSeq:argumentF.TT,ADegree:argumentF.Degree,AScalar:argumentF.Scalar,TargetResult:targetF.TargetResult }]  AS argumentProduct

  TargetResult: CASE WHEN argumentF.Degree=-1 THEN (targetF.MaxN*argumentF.TT*argumentF.Scalar)*targetF.TargetResult ELSE (((targetF.MaxN^argumentF.Degree)*argumentF.TT*argumentF.Scalar)/2)*targetF.TargetResult END      

UNWIND argumentProduct as argumentProductt
WITH [{ResultScalar:CASE WHEN argumentProductt.ADegree=-1 THEN (argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/2 ELSE 
        (argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/4 END,
      ResultDegree: CASE WHEN argumentProductt.ADegree=-1 THEN argumentProductt.TDegree ELSE argumentProductt.ADegree+argumentProductt.TDegree END,
      ResultTwoSeq:argumentProductt.ATwoSeq}] as resultProduct


UNWIND resultProduct as rP
WITH {RScalarList:COLLECT({RScalar:rP.ResultScalar}),RDegree:rP.ResultDegree} as ResultMap
WITH COLLECT(ResultMap.RScalarList) AS RScalar,ResultMap
Unwind RANGE(0,SIZE(RScalar)-1) as ii
Unwind RScalar[ii] as ts
With collect(ts.RScalar) as cts,ResultMap
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,ResultMap
With [ResultMap.RDegree,TotalRScalar] as output
Return output


 MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="8" AND t.twoSeq=32 AND i.MaxN='9'

WITH [{T:t.twoSeq,Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar),TargetResult:((toInteger(i.MaxN)^toInteger(v.Degree)*toFloat(v.Scalar))/2),MaxN:toInteger(i.MaxN)}] as targetFactors

MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate),(tt:TwoSeqFactor)<-[]-(ii) where ii.N="5" AND ii.MaxN='9'

WITH [{TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar)}]
      as argumentFactors,targetFactors

UNWIND argumentFactors as argumentF
UNWIND targetFactors as targetF

WITH [{TargetResult:CASE WHEN argumentF.Degree=-1 THEN (targetF.MaxN*argumentF.TT*argumentF.Scalar)*targetF.TargetResult ELSE (((targetF.MaxN^argumentF.Degree)*argumentF.TT*argumentF.Scalar)/2)*targetF.TargetResult END}]  as ResultProduct 
UNWIND ResultProduct as ResultProductt
return ResultProductt.TargetResult    








.
MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="8" AND t.twoSeq=16 AND i.MaxN='9'
WITH [{T:t.twoSeq,Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}] as targetFactors
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate),(tt:TwoSeqFactor)<-[]-(ii) where ii.N="4" AND ii.MaxN='9'
WITH [{TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar)}]
      as argumentFactors,targetFactors
UNWIND argumentFactors as argumentF
UNWIND targetFactors as targetF
WITH [{TDegree:targetF.Degree,TScalar:targetF.Scalar,ATwoSeq:argumentF.TT,ADegree:argumentF.Degree,AScalar:
CASE WHEN argumentF.Degree=-1 THEN argumentF.Scalar*2 ELSE argumentF.Scalar END}]  AS argumentProduct
UNWIND argumentProduct as argumentProductt
WITH [{ResultScalar:(argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/4,
      ResultDegree: CASE WHEN argumentProductt.ADegree=-1 THEN argumentProductt.TDegree ELSE argumentProductt.ADegree+argumentProductt.TDegree END,
      ResultTwoSeq:argumentProductt.ATwoSeq}] as resultProduct
UNWIND resultProduct as rP
WITH {RScalarList:COLLECT({RScalar:rP.ResultScalar}),RDegree:rP.ResultDegree} as ResultMap
WITH COLLECT(ResultMap.RScalarList) AS RScalar,ResultMap
Unwind RANGE(0,SIZE(RScalar)-1) as ii
Unwind RScalar[ii] as ts
With collect(ts.RScalar) as cts,ResultMap
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,ResultMap
With [ResultMap.RDegree,TotalRScalar] as output
Return output




Call apoc.export.csv.query("
MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N='8' AND i.MaxN='9'
WITH {DS:collect({Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}),T:toString(t.twoSeq)} as targetFactors,i.N as iN, i.MaxN as iM  
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:targetFactors.T}),(tt:TwoSeqFactor)<-[]-(ii) where ii.MaxN='14'
WITH {DS:Collect({TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar) }),Evaluate:ee.Value}  as argumentFactors  ,targetFactors,iN,iM
WITH COLLECT(targetFactors) as targetF,targetFactors.T as tFT,argumentFactors,iN,iM 
WITH COLLECT(argumentFactors) as argumentF,argumentFactors.Evaluate as aFE,targetF,tFT,iN,iM
UNWIND RANGE(0,SIZE(targetF)-1) as ii
WITH targetF[ii].DS as tFDS, argumentF[ii].DS as aFDS,tFT,aFE,iN,iM
UNWIND tFDS as tFDSU
UNWIND aFDS as aFDSU
WITH {afe:aFE,tft:tFT,DS: COLLECT( {  TDegree:tFDSU.Degree,TScalar:tFDSU.Scalar,ATwoSeq:aFDSU.TT,ADegree:aFDSU.Degree,AScalar:CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Scalar*2 ELSE aFDSU.Scalar END })    }  AS argumentProduct,iN,iM
WITH COLLECT(argumentProduct) as argumentProductt,argumentProduct.afe as aPafe,iN,iM
UNWIND RANGE(0,SIZE(argumentProductt)-1) as iii
WITH argumentProductt[iii].DS as aPTds,aPafe,iN,iM
UNWIND aPTds as aPTdsU
WITH {DS:COLLECT({ResultScalar:(aPTdsU.TScalar*aPTdsU.AScalar*aPTdsU.ATwoSeq)/4, ResultDegree: CASE WHEN aPTdsU.ADegree=-1 THEN aPTdsU.TDegree ELSE aPTdsU.ADegree+aPTdsU.TDegree END,ResultTwoSeq:aPTdsU.ATwoSeq}),ET:aPafe} as resultProduct,iN,iM
WITH COLLECT(resultProduct) as rPC,resultProduct.ET as eT,iN,iM
UNWIND RANGE(0,SIZE(rPC)-1) as iI
WITH rPC[iI].DS as rPCDS,eT,iN,iM
UNWIND rPCDS as rPCDSU
WITH {RScalarList:COLLECT({RScalar:rPCDSU.ResultScalar}),RDegree:rPCDSU.ResultDegree,ET:eT} as resultMap,iN,iM
WITH COLLECT(resultMap) as resultMapC,resultMap.ET as eTrm,resultMap.RDegree as rDRM,iN,iM
UNWIND RANGE(0,SIZE(resultMapC)-1) as iII
WITH resultMapC[iII].RScalarList as rSL,eTrm,rDRM,iN,iM
UNWIND RANGE(0,SIZE(rSL)-1) as iIII
UNWIND rSL[iIII] as rSLI
WITH COLLECT(rSLI.RScalar) as cts,eTrm,rDRM,iN,iM
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,eTrm,rDRM,iN,iM
RETURN TotalRScalar,eTrm,rDRM,iN,iM
 ",
"quartets1408.csv",{d:";"})



MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N="8" AND t.twoSeq=16 AND i.MaxN='9'
WITH [{T:t.twoSeq,Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}] as targetFactors
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate),(tt:TwoSeqFactor)<-[]-(ii) where ii.N="4" AND ii.MaxN='9'
WITH [{TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar)}]
      as argumentFactors,targetFactors
UNWIND argumentFactors as argumentF
UNWIND targetFactors as targetF
WITH [{TDegree:targetF.Degree,TScalar:targetF.Scalar,ATwoSeq:argumentF.TT,ADegree:argumentF.Degree,AScalar:
CASE WHEN argumentF.Degree=-1 THEN argumentF.Scalar*2 ELSE argumentF.Scalar END}]  AS argumentProduct
UNWIND argumentProduct as argumentProductt
WITH [{ResultScalar:(argumentProductt.TScalar*argumentProductt.AScalar*argumentProductt.ATwoSeq)/4,
      ResultDegree: CASE WHEN argumentProductt.ADegree=-1 THEN argumentProductt.TDegree ELSE argumentProductt.ADegree+argumentProductt.TDegree END,
      ResultTwoSeq:argumentProductt.ATwoSeq}] as resultProduct
UNWIND resultProduct as rP
WITH {RScalarList:COLLECT({RScalar:rP.ResultScalar}),RDegree:rP.ResultDegree} as ResultMap
WITH COLLECT(ResultMap.RScalarList) AS RScalar,ResultMap
Unwind RANGE(0,SIZE(RScalar)-1) as ii
Unwind RScalar[ii] as ts
With collect(ts.RScalar) as cts,ResultMap
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,ResultMap
With [ResultMap.RDegree,TotalRScalar] as output
Return output









MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N='8' AND i.MaxN='9'
WITH {DS:collect({Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}),T:toString(t.twoSeq)} as targetFactors,i.N as iN, i.MaxN as iM  
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:targetFactors.T}),(tt:TwoSeqFactor)<-[]-(ii) where ii.MaxN='14'
WITH {DS:Collect({TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar) }),Evaluate:ee.Value}  as argumentFactors  ,targetFactors,iN,iM
WITH COLLECT(targetFactors) as targetF,targetFactors.T as tFT,argumentFactors,iN,iM 
WITH COLLECT(argumentFactors) as argumentF,argumentFactors.Evaluate as aFE,targetF,tFT,iN,iM
UNWIND RANGE(0,SIZE(targetF)-1) as ii
WITH targetF[ii].DS as tFDS, argumentF[ii].DS as aFDS,tFT,aFE,iN,iM
UNWIND tFDS as tFDSU
UNWIND aFDS as aFDSU
WITH {afe:aFE,tft:tFT,DS: COLLECT( {  TDegree:tFDSU.Degree,TScalar:tFDSU.Scalar,ATwoSeq:aFDSU.TT,ADegree:aFDSU.Degree,AScalar:CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Scalar*2 ELSE aFDSU.Scalar END })    }  AS argumentProduct,iN,iM
WITH COLLECT(argumentProduct) as argumentProductt,argumentProduct.afe as aPafe,iN,iM
UNWIND RANGE(0,SIZE(argumentProductt)-1) as iii
WITH argumentProductt[iii].DS as aPTds,aPafe,iN,iM
UNWIND aPTds as aPTdsU
WITH {DS:COLLECT({ResultScalar:(aPTdsU.TScalar*aPTdsU.AScalar*aPTdsU.ATwoSeq)/4, ResultDegree: CASE WHEN aPTdsU.ADegree=-1 THEN aPTdsU.TDegree ELSE aPTdsU.ADegree+aPTdsU.TDegree END,ResultTwoSeq:aPTdsU.ATwoSeq}),ET:aPafe} as resultProduct,iN,iM
WITH COLLECT(resultProduct) as rPC,resultProduct.ET as eT,iN,iM
UNWIND RANGE(0,SIZE(rPC)-1) as iI
WITH rPC[iI].DS as rPCDS,eT,iN,iM
UNWIND rPCDS as rPCDSU
WITH {RScalarList:COLLECT({RScalar:rPCDSU.ResultScalar}),RDegree:rPCDSU.ResultDegree,ET:eT} as resultMap,iN,iM
WITH COLLECT(resultMap) as resultMapC,resultMap.ET as eTrm,resultMap.RDegree as rDRM,iN,iM
UNWIND RANGE(0,SIZE(resultMapC)-1) as iII
WITH resultMapC[iII].RScalarList as rSL,eTrm,rDRM,iN,iM
UNWIND RANGE(0,SIZE(rSL)-1) as iIII
UNWIND rSL[iIII] as rSLI
WITH COLLECT(rSLI.RScalar) as cts,eTrm,rDRM,iN,iM
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,eTrm,rDRM,iN,iM
RETURN TotalRScalar,eTrm,rDRM,iN,iM


MATCH (v:VertexNode)<-[]-(i:IndexedBy)-[]->(e:Evaluate),(t:TwoSeqFactor)<-[]-(i) where i.N='8' AND i.MaxN='9'
WITH {DS:collect({Degree:toInteger(v.Degree),Scalar:toFloat(v.Scalar)}),T:toString(t.twoSeq)} as targetFactors,i.N as iN, i.MaxN as iM  
MATCH (vv:VertexNode)<-[]-(ii:IndexedBy)-[]->(ee:Evaluate {Value:targetFactors.T}),(tt:TwoSeqFactor)<-[]-(ii) where ii.MaxN='9'
WITH {DS:Collect({TT:tt.twoSeq,Degree:toInteger(vv.Degree),Scalar:toFloat(vv.Scalar) }),Evaluate:ee.Value}  as argumentFactors  ,targetFactors,iN,iM
WITH COLLECT(targetFactors) as targetF,targetFactors.T as tFT,argumentFactors,iN,iM 
WITH COLLECT(argumentFactors) as argumentF,argumentFactors.Evaluate as aFE,targetF,tFT,iN,iM
UNWIND RANGE(0,SIZE(targetF)-1) as ii
WITH targetF[ii].DS as tFDS, argumentF[ii].DS as aFDS,tFT,aFE,iN,iM
UNWIND tFDS as tFDSU
UNWIND aFDS as aFDSU
WITH {afe:aFE,tft:tFT,DS: COLLECT( {  TDegree:tFDSU.Degree,TScalar:tFDSU.Scalar,ATwoSeq:aFDSU.TT,ADegree:aFDSU.Degree,AScalar:CASE WHEN aFDSU.Degree=-1 THEN aFDSU.Scalar*2 ELSE aFDSU.Scalar END })    }  AS argumentProduct,iN,iM
WITH COLLECT(argumentProduct) as argumentProductt,argumentProduct.afe as aPafe,iN,iM
UNWIND RANGE(0,SIZE(argumentProductt)-1) as iii
WITH argumentProductt[iii].DS as aPTds,aPafe,iN,iM
UNWIND aPTds as aPTdsU
WITH {DS:COLLECT({ResultScalar:(aPTdsU.TScalar*aPTdsU.AScalar*aPTdsU.ATwoSeq)/4, ResultDegree: CASE WHEN aPTdsU.ADegree=-1 THEN aPTdsU.TDegree ELSE aPTdsU.ADegree+aPTdsU.TDegree END,ResultTwoSeq:aPTdsU.ATwoSeq}),ET:aPafe} as resultProduct,iN,iM
WITH COLLECT(resultProduct) as rPC,resultProduct.ET as eT,iN,iM
UNWIND RANGE(0,SIZE(rPC)-1) as iI
WITH rPC[iI].DS as rPCDS,eT,iN,iM
UNWIND rPCDS as rPCDSU
WITH {RScalarList:COLLECT({RScalar:rPCDSU.ResultScalar}),RDegree:rPCDSU.ResultDegree,ET:eT} as resultMap,iN,iM
WITH COLLECT(resultMap) as resultMapC,resultMap.ET as eTrm,resultMap.RDegree as rDRM,iN,iM
UNWIND RANGE(0,SIZE(resultMapC)-1) as iII
WITH resultMapC[iII].RScalarList as rSL,eTrm,rDRM,iN,iM
UNWIND RANGE(0,SIZE(rSL)-1) as iIII
UNWIND rSL[iIII] as rSLI
WITH COLLECT(rSLI.RScalar) as cts,eTrm,rDRM,iN,iM
With REDUCE(tSum=0,ff IN cts | tSum +ff) As TotalRScalar,eTrm,rDRM,iN,iM
RETURN TotalRScalar,eTrm,rDRM,iN,iM


TotalRScalar	eTrm	rDRM	iN	iM
8.0	"64"	4	"8"	"9"
-56.5	"16"	3	"8"	"9"
1368.0	"32"	2	"8"	"9"
1875.0	"8"	0	"8"	"9"
-26.5	"8"	3	"8"	"9"
-12.5	"4"	3	"8"	"9"
-16640.5	"64"	1	"8"	"9"
5061.0	"16"	0	"8"	"9"
670.0	"4"	0	"8"	"9"
4.0	"32"	4	"8"	"9"
601.0	"16"	2	"8"	"9"
-6935.5	"32"	1	"8"	"9"
-256.5	"64"	3	"8"	"9"
263.0	"8"	2	"8"	"9"
33732.0	"64"	0	"8"	"9"
115.5	"4"	2	"8"	"9"
13244.0	"32"	0	"8"	"9"
-120.5	"32"	3	"8"	"9"
0.5	"4"	4	"8"	"9"
1.0	"8"	4	"8"	"9"
3093.0	"64"	2	"8"	"9"
-2847.5	"16"	1	"8"	"9"
2.0	"16"	4	"8"	"9"
-461.5	"4"	1	"8"	"9"
-1152.5	"8"	1	"8"	"9"
















