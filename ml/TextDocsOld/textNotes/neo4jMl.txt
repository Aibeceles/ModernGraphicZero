GraphSage mentions link prediction, lable prediction. is traversal prediction
a relevant ml endevor? 

GraphSAGE is about created embeddings that can then be plugged
into things like link prediction and node classification. It is nice because it
can create embeddings from both random walks between nodes as well as using the 
edges for embeddings.

pathfinding improvements performans improvements and new standard api for pathfinding.

suplabled ata pairs of nodes that are eiter linked or not.
supervised ml - labeled data tast driven 

fastrp node embeddings.

need positive sample negative sample.

call gds.graph.create






Can you treat embeddings from an upstream ML model (e.g. NLP) as graph data and then make downstream inferences or augmented embeddings?

You can use one embedding - or just a feature vector - as an input to GraphSAGE, and we've seen customers and community users leveraging that possibility. We just introduced this in GDS 1.5 :)



Yes. For example, check out here: https://neo4j.com/docs/graph-data-science/current/algorithms/ml-models/node-classification/#algorithms-ml-nodeclassification

My github (with this demo) is https://github.com/AliciaFrame/ML_with_GDS Our engineering github is https://github.com/neo4j/graph-data-science/


Feature engineering is what made GDS Library awesome for Supervised Learning which can't be possible with Python ML Libraries.

What I do is let Neo4j do what it is best at and THEN bring the results into Python. So you can do all of the feature engineering in Neo4j and then pull those results into Python for your modeling needs.

We have just added this self-paced course “Learn how to use Graph Algorithms of the Graph Data Science Library for Neo4j 4.0.” https://neo4j.com/graphacademy/training-iga-40/enrollment/


mcafee module core service

C:\Users\Aibes\ZeppelinDocs\Using GraphSAGE embeddings for downstream classification model by Tomaz Bratanic Towards Data Scienceabc.html

