


MATCH (n:MuNumber) 
WITH n.nBinary as nB, size(n.nBinary) as nBSize,n
WITH substring(nB,1,nBSize-2) as sBsubstring,n
WITH split(sBsubstring,', ') AS listNB,n
WITH n, listNB, toInteger(listNB[0]) + size(listNB) -1 AS muSize WHERE muSize > 6
DELETE n
RETURN listNB,listNB[0],size(listNB),muSize Limit 250


MATCH (n:CreatedBy)
WITH min(n.resultId) AS targetResultID
MATCH (m:CreatedBy {resultId:targetResultID}) <- [] - (d:Dnode)
RETURN m,d

MATCH (n:CreatedBy)
WITH max(n.resultId) AS targetResultID
MATCH (m:CreatedBy {resultId:targetResultID})
RETURN m

MATCH (m:CreatedBy {resultId:88700}) <- [] - (d:Dnode)
RETURN m,d


MATCH (n:MuNumber) 
WITH n.nBinary as nB, size(n.nBinary) as nBSize,n
WITH substring(nB,1,nBSize-2) as sBsubstring,n
WITH split(sBsubstring,', ') AS listNB,n
WITH n,listNB, toInteger(listNB[0]) + size(listNB) -1 AS muSize WHERE muSize > 6
DELETE n
RETURN listNB,listNB[0],size(listNB),muSize Limit 250

The proper way to implement this graph is by taking advantage of multitennance....
Otherwise:
With a new graph  
Sufficiently populate with muNumbers  
Use above to filter only proper muSize

CYPHER runtime=interpreted
MATCH (n:Dnode {wNum:?})
RETURN n.d as denominator, n.n as numerator, n.wNum as wNum, n.determined as determined


From May:

MATCH (d:Dnode), (k:CreatedBy)

MERGE  (d)-[ee:CreatedBye]->(k)

The meaning is determined=1 is determined determined=0 is under determined.

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:0})
WITH  CASE WHEN k.wNum=0 THEN CASE WHEN d.totalZero=5 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:1})
WITH  CASE WHEN k.wNum=1 THEN CASE WHEN d.totalZero=4 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin
 
MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:2})
WITH  CASE WHEN k.wNum=2 THEN CASE WHEN d.totalZero=3 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin 

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:3})
WITH  CASE WHEN k.wNum=3 THEN CASE WHEN d.totalZero=2 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:4})
WITH  CASE WHEN k.wNum=4 THEN CASE WHEN d.totalZero=1 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:5})
WITH  CASE WHEN k.wNum=5 THEN CASE WHEN d.totalZero=0 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

Need wNum on dNode

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:0})
SET d.wNum=k.wNum
RETURN d.wNum

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:1})
SET d.wNum=k.wNum
RETURN d.wNum
 
MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:2})
SET d.wNum=k.wNum
RETURN d.wNum 

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:3})
SET d.wNum=k.wNum
RETURN d.wNum

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:4})
SET d.wNum=k.wNum
RETURN d.wNum

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:5})
SET d.wNum=k.wNum
RETURN d.wNum

MATCH (d:Dnode {wNum:3} )
RETURN d

CYPHER runtime=interpreted
MATCH (n:Dnode) - [] -> (c:CreatedBy {wNum:5})
RETURN n.d as denominator, n.n as numerator, n.wNum as wNum, n.determined as determined

CYPHER runtime=interpreted
MATCH (n:MuNumber )
RETURN toString(n.Value) as value,toString(n.size) as size,toString(n.d) as d,toString(n.n) as n,n.nBinary as nBinary

MATCH (n:Dnode) - [] -> (c:CreatedBy {wNum:4})
WITH DISTINCT n,toString(c.wNum) AS wNum
RETURN toString(n.d) AS denominator, toString(n.n) AS numerator, wNum, toString(n.determined) AS determined, c.skipList AS skipPattern

CYPHER runtime=interpreted
MATCH (n:Dnode) - [] -> (c:CreatedBy {wNum:?})
WITH DISTINCT n,toString(c.wNum) AS wNum,c.skipList AS skipPattern
RETURN toString(n.d) AS denominator, toString(n.n) AS numerator, wNum, toString(n.determined) AS determined, skipPattern

CYPHER runtime=interpreted
MATCH (n:Dnode) - [] -> (c:CreatedBy {wNum:4})
WITH DISTINCT n,toString(c.wNum) AS wNum,c.skipList AS skipPattern
RETURN toString(n.d) AS denominator, toString(n.n) AS numerator, wNum, toString(n.determined) AS determined, skipPattern



MATCH (m:MuNumber {d:'6'})
WITH m.d as mDen
MATCH (n:Dnode {d:toInteger(mDen)}) - [] -> (c:CreatedBy {wNum:0})   
RETURN n


WITH DISTINCT n,toString(c.wNum) as wNum        
RETURN toString(n.d) as denominator, toString(n.n) as numerator, wNum, toString(n.determined) as determined   

CYPHER runtime=interpreted
MATCH (m:MuNumber {d:'6'})
WITH m.d as mDen
MATCH (n:Dnode {d:toInteger(mDen)}) - [] -> (c:CreatedBy {wNum:0})   
WITH DISTINCT n
RETURN toString(n.d) as denominator, toString(n.n) as numerator, wNum, toString(n.determined) as determined

This above script is the topic of public ResultSet wNumDenQ(int wNum)  



MATCH (m:MuNumber) 
WITH m,toInteger(m.d) as denominator,toInteger(m.n) as numerator
OPTIONAL MATCH (d:Dnode {d:denominator,n:numerator})
WITH m,d
FOREACH(x IN (CASE WHEN d IS NULL THEN [] else [1] END) |
  DETACH DELETE m
)


From May:

MATCH (d:Dnode), (k:CreatedBy)

MERGE  (d)-[ee:CreatedBye]->(k)

The meaning is determined=1 is determined determined=0 is under determined.

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:0})
WITH  CASE WHEN k.wNum=0 THEN CASE WHEN d.totalZero=5 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:1})
WITH  CASE WHEN k.wNum=1 THEN CASE WHEN d.totalZero=4 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin
 
MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:2})
WITH  CASE WHEN k.wNum=2 THEN CASE WHEN d.totalZero=3 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin 

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:3})
WITH  CASE WHEN k.wNum=3 THEN CASE WHEN d.totalZero=2 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:4})
WITH  CASE WHEN k.wNum=4 THEN CASE WHEN d.totalZero=1 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy {wNum:5})
WITH  CASE WHEN k.wNum=5 THEN CASE WHEN d.totalZero=0 THEN 1 ELSE 0 END ELSE 2 END as determin,d,k
SET d.determined=determin
RETURN d.totalZero,k.wNum,determin

MATCH p=(d:Dnode {d:4})-[r:predictedLink*8]->(d:Dnode {d:4}) RETURN p

MATCH ()-[r:RELEASED]-() 
DELETE r

MATCH (:Dnode)-[r:predictedLink]->(:Dnode)
DELETE r

MATCH (d:Dnode)-[r:predictedLink]->(dd:Dnode)
RETURN d,dd LIMIT 50

MATCH (:Dnode)-[r:predictedLink]->(:Dnode)
RETURN COUNT(r)

MATCH (d:Dnode)-[ee:CreatedBye]->(k:CreatedBy)
SET d.wNum=k.wNum
RETURN COUNT(D)





















































































































































